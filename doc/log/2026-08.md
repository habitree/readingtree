# 2026-08 Prompt Log — Habitree ReadingTree

---

### 2026-08-04 — 책 검색 소스 교체: 네이버 책 API 종료 → 알라딘 주력 + Google Books 폴백
- **카테고리:** #책검색 #외부API #장애대응
- **중요도:** P0
- **지시:** "책 등록 시 API 정보 오류 발생 확인" (모바일에서 `Invalid search api (존재하지 않는 검색 api 입니다.)` 토스트)
- **원인 — 앱 버그가 아니라 네이버의 서비스 종료:** `search/book.json`·`search/book_adv.json` 모두 `SE05 Invalid search api`. **같은 키로 `search/encyc.json` 은 정상 응답** → 키·쿼터·네트워크 문제가 아니라 네이버가 책 검색만 내린 것으로 확정. 책 등록의 유일한 검색 소스였기에 기능이 통째로 멎었다.
- **보유 키 실측 인벤토리(값 미노출):** 알라딘 ✅(검색 927건) · Google Books ✅(300건) · **국중도 ❌ `ERR_CODE 011`** · **정보나루 ❌ 인증 실패 + 코드 미사용**. 즉 책 검색에 즉시 쓸 수 있는 소스는 알라딘·Google Books 둘뿐. 카카오 책 검색은 키 신규 발급이 필요해 이번 범위에서 제외.
  - **부수 발견 → 원인은 만료가 아니라 `.env.local` 오기입:** 페이지 수 폴백 1순위인 국중도가 죽어 있었다. 처음엔 키 만료/폐기로 봤으나, 발급 화면(`Open API > 인증키 신청/관리`)을 열어보니 **키는 "승인" 상태로 멀쩡했고 `.env.local` 값이 56자 — 실제 발급키는 64자로 잘려 저장**돼 있었다. 화면의 키로 교체하니 즉시 부활(`504p`/`727p`, `source=nl_seoji`). **재발급 불필요.**
  - **교훈:** `011 유효하지 않은 인증키`는 "키가 죽었다"가 아니라 "보낸 값이 발급값과 다르다"까지 포함한다. 키 장애를 만나면 **발급처 화면의 값과 길이부터 대조**할 것.
- **교체(`lib/api/book-search.ts` 신설, `lib/api/naver.ts` 삭제):** 알라딘 `ItemSearch` 우선 → **0건일 때만** Google Books. 종전 네이버 클라이언트와 **같은 반환 모양**(`{total,start,display,items}` + `searchBooks`/`transformBookItem`)을 유지해 호출부 6곳은 import·식별자만 교체.
  - 소스별 차이 흡수: 페이징(네이버식 1-based 아이템 인덱스 → 알라딘 페이지 번호 / Google `startIndex`), ISBN(알라딘 `isbn13` 우선, Google `ISBN_13`→`ISBN_10`), 표지 http→https, 상한(알라딘 50·Google 40).
  - **출간일**: 소스마다 `2016-07-25`/`2016-07`/`2016` 이 섞여 와 **일 단위까지 확정된 값만 통과**시킨다(부분 날짜를 임의로 1일로 채우지 않음 — 기존 네이버 8자리 처리와 같은 보수성).
  - 두 소스가 **모두 장애**일 때만 예외를 던지고, 단순 0건은 빈 결과로 둔다(검색 실패와 결과 없음을 구분).
  - `next.config.js` remotePatterns 에 `books.google.com` 추가(폴백 표지).
- **검증:** `/api/books/search` 실호출 — 한국 도서(927건·표지·ISBN13 정상) / ISBN 검색 / 영문 제목 모두 정상. 폴백 3경로 직접 확인: 알라딘 정상 `source=aladin` → 알라딘 키 제거 시 `source=google_books`(ISBN13·표지 정상) → 양쪽 0건이면 빈 결과. tsc·eslint(신규 파일 무경고)·`next build` 통과.
- **정리:** 미사용 `DATA4LIBRARY_AUTH_KEY` 를 `.env.local` 에서 제거(코드 어디에도 참조 없음 + 인증 실패). **Vercel 환경변수에도 남아 있다면 별도 정리 필요.**
- **변경 파일:** `lib/api/book-search.ts`(신규), `lib/api/naver.ts`(삭제), `app/api/books/search/route.ts`, `app/api/books/search/batch/route.ts`, `app/actions/books/progress.ts`, `app/actions/books/reading.ts`, `app/actions/ai/summarization.ts`, `lib/ai/utils/book-registration.ts`, `next.config.js`.

---

### 2026-08-04 — 배경음악 재생 중 영구 정지 수정: 전환 실패 시 무음 고착 제거
- **카테고리:** #음악 #백색소음 #버그픽스
- **중요도:** P1
- **지시:** "음악 소리가 중간에 계속 재생되지 않고 끊기는 현상 — 최근 추가된 설정으로 발생한 것으로 보임. 점검 후 문제없이 재생되도록 업데이트."
- **증상 확인:** 사용자 확인 결과 "**재생 도중 완전히 멈춤(복구 안 됨)**", "**백색소음 채널에서 주로 발생**". 곡 전환 시 무음 갭이 아니라 영구 정지.
- **아닌 것부터 배제:** 음원 파일 107곡 전수 검사(ffprobe) — 메타 duration ↔ 실측 일치, VBR Xing/LAME 헤더 전부 존재(브라우저 duration 오판 없음). 프로덕션 CDN도 정상(`/music/*` 200, Content-Length 일치, Accept-Ranges·immutable). **파일·인코딩·배포는 무관.** 로컬 dev 실기 관찰에서도 자연 전환 6회 연속 정상 → 정상 경로에는 문제가 없고 **실패 경로에 복구 수단이 없는 것**이 원인.
- **원인 ①(주범, 실기 재현 완료):** `goToTrack`의 `startNew` 가 `el.volume = 0` 으로 놓고 `el.play().then(페이드인).catch(() => {})` — **play() 거부를 통째로 삼킨다.** 이 시점에 `activeIdx` 는 이미 스왑돼 해당 엘리먼트가 활성이므로, 볼륨 0 + 정지 상태로 남으면 timeupdate·ended·stalled 가 하나도 발생하지 않아 **자력 복구 경로가 전무**하다. UI 는 계속 "재생 중". `beginTrack` 경로는 `safePlay`(지수 백오프 재시도)를 타서 살아나지만 `startNew` 만 무방비였다.
  - **재현:** 프리로드 완료 후 스킵 시점의 play() 를 1회 거부시키자 구 코드는 **양쪽 엘리먼트 정지·볼륨 0 으로 25초+ 영구 무음**, 수정본은 **10ms 내 볼륨 복구 + 재생 재개**.
- **원인 ②(전환 교착):** 딥(구곡 페이드아웃) 후의 신곡 시작을 `rampVolume` 의 **완료 콜백**에만 의존. 램프는 단일 `fadeTimer` 를 공유해 다른 페이드가 끼어들면 `clearFade()` 로 취소되는데, 그때 완료 콜백이 영영 실행되지 않아 `isTransitioning` 이 **true 로 고착** → `handleTimeUpdate`·`handleEnded` 가 모두 첫 줄에서 return → 프리로드도 다음 곡 전환도 불가 → 영구 정지. (실경로: 프리로드 미완으로 대기 중이던 `beginTrack` 의 canplay 가 딥 도중 늦게 발화 → `startFadeIn` → `clearFade`.)
- **원인 ③(무음 구간):** 볼륨 램프 진행도를 **틱 횟수**로 계산 — 백그라운드 탭에서 setInterval 이 1초까지 throttle 되면 400ms 페이드인이 수십 초로 늘어나 그동안 사실상 무음.
- **백색소음에서 잦았던 이유:** 곡이 짧아 전환 횟수가 압도적이다. 모닥불 58초 = 시간당 62회 전환(음악 5분 곡의 5배). 전환마다 발동하는 결함이라 짧은 채널일수록 먼저·자주 드러난다.
- **수정(`music-mini-player.tsx` 단일 파일):**
  - `startNew` 의 play() 거부를 catch 에서 **볼륨 원복 + `safePlay` 재시도**로 인계(원인 ①).
  - 딥 후 전환을 볼륨 램프와 분리 — `scheduleAfterDip`(독립 setTimeout)로 예약해 **램프가 취소돼도 전환은 반드시 실행**(원인 ②).
  - `rampVolume` 진행도를 **경과 시간 기반**으로 전환 — 틱이 늦어도 페이드가 늘어지지 않는다(원인 ③).
  - **재생 워치독**(2s): 재생 중이어야 하는데 활성이 정지면 `safePlay`, 페이드 중이 아닌데 볼륨이 목표와 어긋나면 원복, 전환이 5초를 넘기면 `isTransitioning` 강제 해제. 원인 미상의 정지까지 덮는 최종 안전망.
- **검증:** Playwright 실기 — 구 코드/수정본에 **동일 주입·동일 지점** 대조 실험(위 재현), 자연 전환(ended) 회귀 없음, 채널 시작·스킵 정상. music vitest 27개, tsc·eslint·`next build` 통과.
- **변경 파일:** `components/music/music-mini-player.tsx`.

---

### 2026-08-20 — 기록 시트에 % 입력 추가: 이북처럼 페이지를 모를 때도 진행 기록
- **카테고리:** #기록 #진행률 #이북 #백로그
- **중요도:** P1
- **지시:** `.backlog/0-inbox/_raw/기록기능 추가.md` — "기록의 페이지기록시 p숫자와 별도로 %도 입력할수있는 기능 / 이북으로 볼경우 원페이지를 모를경우가있기에 %로 업데이트". REQ-0008로 인테이크.
- **현황 파악:** 앱의 진행 축은 **정수 페이지 단일 기준**이고(`reading_logs.start_page/end_page`, `user_books.current_page`), %는 `lib/reading/progress.ts` 의 `computeProgressPercent()` 가 만드는 **출력 전용 파생값**이었다. 즉 %는 이미 있었지만 입력 경로가 없었다.
- **문제:** 이북 뷰어는 종이책 페이지 대신 %만 보여주는 경우가 많다. 그래서 페이지를 추측해 넣거나 0으로 두고 시간만 기록하게 되는데, 후자는 페이스 집계에서 `timeOnly` 로 빠져 **독서 속도 통계가 빈다.**
- **설계 결정 3건(사용자 확인):**
  - **저장 방식 = 환산 저장.** % 입력을 `round(total_pages × %/100)` 로 페이지 변환해 **기존 컬럼에 그대로** 넣는다. DB 마이그레이션 없음. 페이스·통계·공유·결산은 손대지 않는다. 대안이던 "% 원본 컬럼 추가"는 초/페이지 페이스부터 공유 카드까지 이중 단위 처리를 낳아 기각.
  - **`total_pages` 없는 책 = 총 페이지 먼저 입력 유도.** `%` 탭을 누르면 % 입력 대신 안내 + 기존 `TotalPagesEditor` 트리거를 노출하고, 저장되면 곧바로 % 입력이 열린다. "총 100p 가정"은 페이스 통계를 왜곡하므로 기각.
  - **범위 = 기록 시트 시작·끝 2곳만.** 사후 첨부·북마크 페이지·책 상세 진행률·독서속도 상세는 이번 범위 밖.
- **구현:** `percentToPage()` 를 `computeProgressPercent()` 와 **같은 파일에** 두어 진행률 환산의 단일 출처를 유지. p/% 전환 입력은 두 스텝이 공유하도록 `record-page-input.tsx` 한 곳에만 만들고, 저장 단위는 언제나 페이지로 정규화해 호출부(`onChange(page)`)는 % 존재를 모른다.
  - 끝 %가 시작 페이지보다 앞선 페이지로 환산되는 경우는 **막지 않고** 기존 저장 검증("끝 페이지는 시작 페이지 이상이어야 합니다")에 그대로 걸리게 뒀다 — 조용히 스냅시키면 사용자가 잘못 입력한 사실을 모른다.
  - 반올림 손실은 최대 0.5페이지, 왕복(57% → 182p → 57%) 안정.
- **검증:** tsc·eslint(변경 4파일 무경고)·`next build` 통과. **수동 UI 확인 3건(320p 책 % 입력 → 182p 저장 / 총 페이지 없는 책 유도 / `p` 탭 기본 유지)은 미실시 — 사용자 확인 필요.**
- **변경 파일:** `lib/reading/progress.ts`, `components/records/record-page-input.tsx`(신규), `components/records/record-start-step.tsx`, `components/records/record-end-step.tsx`.

---

### 2026-08-24 — AI 리포트 이미지 공유 카드: 템플릿 10종 제안 → 5종 적용, 블로그 공유를 "완성 이미지 복사"로 전환
- **카테고리:** #AI리포트 #공유 #디자인 #템플릿
- **중요도:** P1
- **지시:** "AI 리포트를 네이버 블로그에 그대로 붙이면 어색함 → 완성된 전체 이미지로 복사해 붙여넣게. 실제 책(`나는 정말 운이 좋은 사람입니다`) 데이터 기반으로 디자인 업체 수준 템플릿 10종 컨셉 제작 → 선택 → 적용." 사용자가 쇼케이스에서 **T03·T06·T07·T08·T10 5종 선택**(당초 3종 계획에서 확대).
- **배경:** 기존 블로그 공유는 `blog-html-builder` 인라인 HTML 복사인데 스마트에디터가 스타일을 일부 제거해 어색하게 붙는다. 콘텐츠 템플릿(톤·섹션, `report_templates`)과 별개로 **시각(공유 카드) 템플릿 축**을 신설.
- **진행:** ① 워크플로우 1차(리서치 4 + 디자인 10 에이전트) — 에디토리얼·뮤직/시네마·아날로그 페이퍼·한국 감성 4개 도메인 웹 리서치 후 실데이터로 10종 HTML 제작 → 쇼케이스 아티팩트 발행. ② 사용자 5종 선택. ③ 워크플로우 2차(5 에이전트 병렬) — HTML을 데이터 바인딩 React 컴포넌트로 포팅(가변 데이터·미완독·표지 없음 분기 포함).
- **구현:**
  - `share-card-data.ts` — 리포트 마크다운 → `ShareCardData` 정규화(인사이트/인용+쪽/독자 문장/여정/요약+마지막 질문 분리, 읽는 이유 추출). 파싱은 `report-parser`·`report-magazine` 재사용.
  - 템플릿 5종(`templates/t03~t10`) — `.tpl-XX` 스코프 CSS + 800px 고정폭, html2canvas 안전 제약(세로쓰기·filter·blend 금지) 준수. 신문(T03)·손편지(T06)·LP 라이너 노트(T07)·계절 수채(T08, 월→계절 동적)·킨포크 에디토리얼(T10).
  - `share-card-dialog.tsx` — 템플릿 선택 + zoom 미리보기 + **화면 밖 800px 원본 노드 캡처**(`captureElementToPngBlob` targetWidth 1600) → `copyImagePromiseToClipboard`(Safari 제스처 대응), 미지원 시 `downloadImage` 폴백. `share-card-fonts.ts` 로 Google Fonts 온디맨드 로드(전역 번들 오염 없음).
  - 리포트 액션바(`reading-report-content.tsx`)에 "이미지 카드" 버튼 추가. **기존 "블로그 복사"(HTML)는 유지.**
- **함정 2건(재발 주의):**
  - 전역 `max-width:100%` + Radix DialogContent 의 `transform`(fixed 자손의 컨테이닝 블록화)이 겹쳐 **캡처 노드가 800→678px로 축소**돼 저해상도·레이아웃 변형 캡처가 됨 → 캡처/미리보기 노드에 인라인 `maxWidth:"none"` 필수.
  - 다이얼로그는 포털로 늦게 마운트돼 `useRef`+`useEffect(open)` 로는 미리보기 배율 측정 실패 → **callback ref** 로 노드 확보 후 ResizeObserver.
- **검증:** tsc·eslint·`next build` 통과. 로컬 실기(임시 dev 페이지, 검증 후 삭제) — 5종 전부 실데이터 렌더 확인, 캡처 파이프라인 실행으로 1600px PNG 생성·픽셀 검증(신문·수채: 폰트/표지 프록시/그라데이션/세로 글자 스택 보존), 저장·토스트 동작 확인. **미실시: 네이버 블로그 실붙여넣기·모바일(iOS Safari) — 사용자 확인 필요.**
- **변경 파일:** `components/books/share-card/`(신규 9파일: share-card-dialog.tsx, share-card-data.ts, share-card-fonts.ts, templates/{types,format,index}.ts, templates/{t03-newspaper,t06-letter,t07-liner-notes,t08-watercolor,t10-editorial}.tsx), `components/books/reading-report-content.tsx`.

---

### 2026-08-24 — 관리자 리포트 툴에 "양식 미리보기" 탭 신설 — 업데이트된 리포트 양식(매거진 본문 + 이미지 카드 5종)을 관리자 페이지에서 확인
- **카테고리:** #관리자 #AI리포트 #미리보기
- **중요도:** P2
- **지시:** "업데이트된 AI 리포트 툴을 확인할 수 있는 기능 — 기존 관리자의 리포트 툴을 정리하고 업데이트된 양식을 확인할 수 있는 페이지로 변경."
- **배경:** 이미지 공유 카드 5종 추가(같은 날 P1 작업) 후에도 관리자 리포트 툴(`/admin/report-settings`)에는 실사용자 노출 양식을 볼 방법이 없었다. 템플릿 빌더의 미리보기는 AI 생성용 섹션 구조 모형일 뿐.
- **구현:** `report-preview-panel.tsx` 신규 — 고정 샘플 데이터(아주 작은 습관의 힘, 6섹션 마크다운)로 ①이미지 카드 5종(템플릿 선택 + zoom 축소 미리보기, 서체 온디맨드 로드) ②매거진 본문(`ReadingReportMagazine`) 전환 뷰. **실사용자 화면 컴포넌트를 그대로 재사용** — 별도 모형 아님. 설정 패널은 탭 5개로 재편(기본 탭 = 양식 미리보기), 기존 템플릿·모델·생성·통계 탭은 AI 생성 프롬프트에 여전히 쓰이므로 유지.
- **재사용한 함정 대응:** 카드 미리보기 노드에 `maxWidth:"none"`(전역 max-width 축소 방지) + callback ref 배율 측정(탭 콘텐츠 지연 마운트) — 공유 카드 다이얼로그와 동일 패턴.
- **검증:** tsc·eslint(신규 파일 0건) 통과. **미실시: 관리자 로그인 후 실렌더 확인 — 사용자 확인 필요.** (report-settings-panel의 기존 미사용 import 경고 5건은 이전부터 존재 — 미수정 보고만)
- **변경 파일:** `components/admin/report-preview-panel.tsx`(신규), `components/admin/report-settings-panel.tsx`, `app/(main)/admin/report-settings/page.tsx`.

---

### 2026-08-24 — 리포트 생성 페이지에 간단 스타일 선택 도입: 진입 즉시 자동 생성 제거
- **카테고리:** #AI리포트 #UX #템플릿
- **중요도:** P2
- **지시:** "AI 리포트 생성 페이지에서 템플릿 선택을 간단하게 하고 생성되도록 업데이트."
- **현황 파악:** 리포트 페이지는 진입 즉시 `generateReadingReport()` 자동 실행(한도·비용 소모)이고 템플릿 선택 UI가 없었다. `ReportTemplateSelector`(DB 콘텐츠 템플릿 12종 드롭다운)는 어디에도 연결 안 된 죽은 컴포넌트. "복잡한 선택지"의 정체는 이 콘텐츠 템플릿 12종.
- **설계 결정:** 선택지는 **이미지 카드 5종**(신문·손편지·LP·수채·킨포크)으로 단순화. 콘텐츠 템플릿과의 매핑은 **하지 않음** — v2 스타일들은 4섹션 구조라 표준 6섹션 기반 공유 카드가 비게 됨(editorial-v2엔 thoughts/journey 없음 등). 생성은 기존 기본 템플릿 유지, 선택한 스타일은 '이미지 카드' 다이얼로그의 초기 템플릿으로 연결.
- **구현:** `report-style-picker.tsx`(신규) — 5카드 그리드 + "AI 리포트 생성" CTA. `reading-report-content.tsx` — 자동 생성 useEffect 제거, 미생성 상태에서 피커 노출, 선택값을 `ShareCardDialog`의 신규 `initialTemplateId` prop으로 전달. 수채 태그라인의 샘플 전용 문구("봄의 한강…")를 범용 문구로 교체.
- **부수 효과(의도됨):** 페이지 진입만으로 AI 한도가 소모되던 문제 해소 — 생성은 명시적 버튼 클릭 후에만.
- **검증:** tsc·eslint·`next build` 통과. 임시 페이지(검증 후 삭제)로 선택 전환·생성 트리거·다크 테마 렌더 실기 확인. 저장 리포트 열람(view=saved)·재생성 흐름은 변경 없음.
- **변경 파일:** `components/books/share-card/report-style-picker.tsx`(신규), `components/books/reading-report-content.tsx`, `components/books/share-card/share-card-dialog.tsx`, `components/books/share-card/templates/t08-watercolor.tsx`.

---

### 2026-08-24 — 관리자: 회원별 접속기록 조회 + 접속기록 화면 메뉴명 표시 (REQ-0010)
- **카테고리:** #관리자 #접속추적 #회원관리 #백로그
- **중요도:** P1
- **지시:** "가입된사용자의 접속기록 확인가능하도록 기능업데이트 적용 그리고 관리페이지에 페이지설명등은 실제 메뉴명으로 인지하기 쉽게 업데이트"
- **현황 파악:** `login_logs`·`access_logs` 테이블과 기록 함수는 이미 완비. `/admin/tracking` 은 IP 기준 필터만 있어 **특정 회원 한 명의 접속 이력을 모아 볼 방법이 없었고**, 접속 로그·페이지뷰 랭킹은 원시 경로(`/books/uuid`)로만 표시돼 어떤 메뉴인지 알기 어려웠다. 메뉴명 매핑(`MENU_LABELS`)은 `tracking.ts`("use server" 파일) 내부에 갇혀 있어 클라이언트에서 재사용 불가.
- **구현 (DB 변경 없음):**
  - **회원별 접속기록:** 회원 관리(`/admin/users`) 목록에서 회원 행 클릭 → 다이얼로그로 요약(총 페이지뷰·로그인 횟수·마지막 접속) + 페이지 접속 기록 100건 + 로그인 기록 50건. 신규 서버 액션 `getMemberAccessHistory(userId)` 는 `requireAdmin()` 선행 + 4개 쿼리 전부 `user_id` 필터.
  - **메뉴명 표시:** `MENU_LABELS` 를 `lib/constants/page-labels.ts` 로 승격하고 실제 라우트 기준 13개 추가(AI 채팅·검색·설정·스탬프·독서 성향·포인트·월간 결산 등 총 29개). 공용 `PathLabel` 컴포넌트로 접속 추적의 접속 로그·페이지뷰 랭킹과 회원별 다이얼로그에서 **메뉴명 주 표기 + 원시 경로 보조 표기**. 메뉴 사용 분석도 미매핑이던 메뉴들이 한글명을 얻음.
- **함정 1건:** 다이얼로그의 effect 내 동기 `setLoading(true)` 이 `react-hooks/set-state-in-effect` 에러 → fetch 결과를 `{memberId, data}` 로 저장하고 로딩을 파생값(`member && result?.memberId !== member.id`)으로 계산하는 패턴으로 수정.
- **검증:** tsc·eslint(변경 파일 오류 0) 통과. 브라우저 실동작(다이얼로그 오픈·데이터 표시)은 미확인 — 사용자 확인 필요. 범위 밖 발견: `tracking.ts` `providerMap` prefer-const 경고(기존 코드, 미수정).
- **변경 파일:** `lib/constants/page-labels.ts`(신규), `components/admin/path-label.tsx`(신규), `components/admin/member-access-dialog.tsx`(신규), `app/actions/admin/tracking.ts`, `components/admin/members-list.tsx`, `components/admin/tracking-dashboard.tsx`.

---

### 2026-08-24 — 스타일 선택에 실물 미리보기 썸네일 + 리포트 본문을 선택 양식으로 렌더
- **카테고리:** #AI리포트 #UX #템플릿
- **중요도:** P2
- **지시:** "템플릿 예시 이미지를 선택할 때 인지할 수 있게 심플하게 보여주고, 선택한 양식이 생성된 리포트에 반영되지 않는 문제 점검·적용."
- **원인:** 선택 스타일이 '이미지 카드' 다이얼로그의 기본값에만 연결되고, 생성 후 리포트 본문은 항상 기존 매거진(다크그린) 뷰로 렌더 → 사용자에게는 "양식 미반영"으로 보였다.
- **구현:**
  - `template-scaled-view.tsx`(신규) — 800px 템플릿을 컨테이너 폭에 맞춰 zoom 축소하는 공용 `TemplateScaledView` + 상단부 크롭 `TemplateThumb`.
  - 스타일 피커 카드에 **이 책의 실제 데이터(제목·저자·표지)로 렌더한 미니 미리보기** 삽입 — 정적 예시 이미지 대신 라이브 렌더(빈 리포트 데이터 → 각 템플릿 헤드만 노출)라 자산 관리 불필요·템플릿 수정 시 자동 동기화.
  - 생성 성공 시 본문을 **선택한 템플릿 양식으로 직접 렌더** + 상단 스타일 전환 칩 5개(즉시 전환). 기존 매거진 뷰는 bookInfo 없는 폴백으로만 유지. 액션 바(책으로/새로 생성/저장/이미지 카드/공유)는 공용 변수로 추출해 양쪽 재사용.
  - 페이지 칩 전환 → 다이얼로그 기본 선택 동기화(렌더 중 파생 상태 보정 패턴 — `set-state-in-effect` 린트 준수).
- **검증:** tsc·eslint·`next build` 통과. 임시 페이지(검증 후 삭제)로 썸네일 5종 식별성·칩 전환·수채 본문 렌더 실기 확인.
- **변경 파일:** `components/books/share-card/template-scaled-view.tsx`(신규), `report-style-picker.tsx`, `share-card-dialog.tsx`, `components/books/reading-report-content.tsx`.
