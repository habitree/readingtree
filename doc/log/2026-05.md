# 2026-05 Prompt Log — Habitree ReadingTree

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 7 완료 + 본 개편 종료 (텔레메트리 4종)
- **카테고리:** #구현 #DB #관측
- **중요도:** P0
- **지시:** "이어서 진행"
- **맥락:** Phase 6까지 모든 데이터 모델·액션·UI·진입점·legacy 모니터링 완료. Phase 7은 운영 단계의 관측을 위한 이벤트 수집 인프라 + 본 개편 마무리 선언.
- **결과:**
  - **`migration-202605041100__tracking__record_events.sql` 신설 + 적용**
    - Supabase MCP `apply_migration` (tracking_record_events)
    - 컬럼: id/user_id(FK auth.users SET NULL)/event/session_id/metadata/created_at
    - CHECK: event ∈ {record_started, record_ended, record_abandoned, detail_added}
    - 인덱스 2종: (user_id, event, created_at DESC), (event, created_at DESC)
    - RLS 3개 정책: service_role select·insert + authenticated select_own
    - 검증: table_exists 1, rls_enabled 1, policies 3, indexes 2
  - **`app/actions/tracking/records.ts` 신설** — `recordRecordEvent({event, userId, sessionId?, metadata?})`. share_events 패턴(무음 실패 + admin 클라이언트).
  - **`app/actions/sessions.ts` 4개 액션 텔레메트리 통합**
    - `startReadingSession` → `record_started` (has_book·has_target_seconds·target_seconds·start_page)
    - `endReadingSession` → `record_ended` (duration_s·pages_read·has_memo·has_bookmark·photo_count·promoted_to_stamp·reached_end·points_earned)
    - `cancelActiveSession` (≥30s) → `record_abandoned` (duration_s·source='manual')
    - `reapOrphanSessions` → 정리된 각 세션마다 `record_abandoned` (source='auto_12h')
    - `addNoteToSession` → `detail_added` (detail_kind·has_session_link·content_length·has_image)
  - **검증**: `npm run type-check` ✅, vitest 본 개편 누적 20/20 ✅, RLS·CHECK·INDEX 모두 통과
  - **미진행 항목** (옵션·후속, 본 phase 종료에 영향 없음):
    - `reading_records_view` UNION view — 데이터 모델 정리로 이미 reading_logs 단일 정본, 추가 가치 작음
    - 통계 페이지 갱신 (스탬프 비율·페이스 차트·캘린더 히트맵) — 광범위 UI 작업, 별도 PRD
    - 4개 이벤트 도달률 90%+ / 에러율 < 0.5% 검증 — 카나리 운영 후 모니터링

---

### 본 개편 종료 선언 — 기록 기능 전면 개편 v2

**완료 (Phase 0~7)**
| Phase | 산출물 | 상태 |
|---|---|---|
| 0 | 기획 문서 13종 (`doc/update/기록기획/`) | ✅ |
| 1 | 마이그 4종 + 타입 확장 | ✅ |
| 2 | 세션 액션 6종 + 라이브 훅 + vitest 14 | ✅ |
| 3 | RecordSheet 7컴포넌트 + 훅 + vitest 6 | ✅ |
| 4 | 인디케이터 (FAB·헤더·D5 별도 영역) | ✅ |
| 5 | 진입점 통합 + 카나리 토글 + legacy @deprecated | ✅ |
| 6 | orphan 정리 + photo/progress 모니터링 + attach 강화 | ✅ |
| 7 | record_events 4종 텔레메트리 | ✅ |

**누계 산출물**
- DB: 마이그 6개 적용 (4 컬럼/트리거 + 1 backfill + 1 orphan + 1 record_events), 신규 컬럼 8 + 인덱스 6 + 트리거 1 + 테이블 1
- 코드: 신규 11파일 (sessions.ts, use-reading-session, use-record-sheet, records/* 7, active-session-indicator, feature-flags, tracking/records), 수정 8파일
- 테스트: vitest 20/20 (sessions 14 + record-sheet 6)
- 문서: 기록기획 13개 + 로그 7개 엔트리 + milestone 1

**현재 상태**
- 카나리 OFF 기본값 (`NEXT_PUBLIC_RECORD_V2` 미설정) → 사용자 노출 0
- 새 RecordSheet·세션 액션·인디케이터는 코드에 mounted, 토글 ON 시 즉시 활성
- legacy 액션·진입점은 그대로 보존 — 즉시 롤백 가능

**카나리 활성화 절차**
1. `.env.local`에 `NEXT_PUBLIC_RECORD_V2=1` → 내부 dev/staging 검증
2. Vercel env (production) 분기 → 신규 가입자 10%
3. 1주 안정 + photo/progress 신규 0건 확인
4. 후속 phase로 진입: legacy 강제 차단 + thin wrapper + shim + view + 통계 페이지

**후속 작업 (별도 PRD)**
- legacy 강제 차단 (createNote photo/progress throw)
- saveReadingSession/createReadingStamp/createQuickNote thin wrapper화
- useStampCaptureStore → useRecordSheet shim
- 통계 페이지 새 모델 기반 갱신 (스탬프 비율·페이스·캘린더 히트맵)
- 사진 보너스·다독서 N세션 등 ADR 부록 미정 사안

**인사이트:** 단계별 카나리 토글로 100% 무중단 점진 이행 가능. 데이터 모델은 prod에 완전 적용되었으나 사용자 흐름은 토글로 보호 — 운영 데이터 학습 후 강제 차단 단계로 옮겨갈 수 있는 구조. 본 개편의 핵심 가치는 "버튼 분류 단순화 (세션 + 상세 2종)"이며, 향후 음성·동영상 등 모달리티 확장 시 같은 축에 흡수 가능.

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 6 완료 (orphan 정리 + 모니터링 + attach 강화)
- **카테고리:** #구현 #DB #모니터링
- **중요도:** P1
- **지시:** "Phase 6 진행"
- **맥락:** Phase 5 카나리는 OFF 기본값. 차단·thin wrapper는 운영 데이터 부재로 위험 → 본 phase는 안전 범위로 한정 (마이그 + 모니터링 + 강화). 강제 차단·shim·wrapper는 카나리 운영 후 별도 phase로 분리.
- **결과:**
  - **`migration-202605040500__data__close_orphan_in_progress.sql` 신설 + 적용**
    - Supabase MCP `apply_migration` (close_orphan_in_progress_sessions)
    - 12시간 이상 in_progress 세션 → abandoned 자동 전환 (idempotent)
    - 검증: orphan_remaining 0건, in_progress_total 0건, in_progress_per_user_max 0
  - **`createNote` photo/progress 사용 모니터링** (app/actions/notes.ts:30)
    - data.type === 'photo' | 'progress' 호출 시 `console.warn([deprecation] ...)` 발송
    - Vercel logs로 호출처·빈도 추적
    - **차단은 안 함** — 호출처 4개(reading-progress, progress-record-sheet, note-form-new, use-note-form) 모두 활성. 카나리 운영 데이터 모이면 별도 phase에서 강제 차단
  - **`attachStampToLog` 강화** (app/actions/progress.ts:893)
    - update payload에 `image_urls: [input.image_url]` 명시 추가
    - DB 트리거가 image_url[0] ↔ image_urls 미러링 책임지지만 코드에서도 명시 — 트리거 비활성·롤백 시에도 정합성 보장
  - **검증**: `npm run type-check` ✅, vitest 본 개편 누적 20/20 ✅, 본 phase 변경 라인 lint 깨끗 (사전 회귀 15 warning은 본 작업 무관)
  - **본 phase 미진행 항목** (위험 회피, 후속 phase 6.5/별도 PR로 분리):
    - createNote에서 photo/progress throw — 호출처 4개 영향, 카나리 데이터 후
    - saveReadingSession/createReadingStamp/createQuickNote thin wrapper화 — 시그니처 매핑 검증 필요
    - useStampCaptureStore의 RecordSheet shim — 카나리 ON 후 호출처 마이그레이션 동시 진행
- **인사이트:** Phase 5 카나리 토글이 OFF 기본값인 상태에서 Phase 6 차단을 강행하면 100% 사용자가 즉시 영향받음(역효과). 모니터링 단계를 두어 호출 빈도·패턴을 학습한 후 차단/wrapper로 전환하는 것이 안전. 본 phase의 마이그·attach 강화는 토글 무관하게 즉시 가치 — 데이터 정합성과 사후 사진 첨부 안정성 확보. 다음은 Phase 7 (텔레메트리 4종 + 통합 뷰 + 통계 페이지).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 5 완료 (진입점 통합 + 카나리 토글 + legacy deprecation)
- **카테고리:** #구현 #리팩토링 #카나리
- **중요도:** P0
- **지시:** "검증 검토 확인후 다음단계 진행"
- **맥락:** 본 개편의 가장 영향 큰 단계. 사용자 노출이 시작되므로 기본은 OFF (NEXT_PUBLIC_RECORD_V2 미설정), 토글 ON일 때만 새 RecordSheet 진입. 기존 진입점·StampCaptureSheet는 그대로 보존하여 즉시 롤백 가능.
- **결과:**
  - **`lib/feature-flags.ts` 신설** — `isRecordV2Enabled()` 단순 환경변수 기반 토글. NEXT_PUBLIC_* 인라인으로 SSR/CSR 일관 (hydration mismatch 0). truthy 값 집합 `{1, true, yes, on}` 매핑.
  - **`components/layout/mobile-nav.tsx` 수정** — handleNoteAction이 카나리 ON일 때 `useRecordSheetStore.openStart({ book })`, OFF일 때 기존 stamp-capture 폴백. continueBook을 RecordSheetBook 형태로 변환하여 양쪽에 동일 prefill 공급.
  - **`components/dashboard/sections/mobile-quick-actions.tsx` 수정** — 모바일 quickAction의 stamp 슬롯 + 데스크톱 DesktopQuickActions stampCapture 슬롯 모두 카나리 분기. React 19 Compiler가 자동 메모화하므로 `useCallback` 제거(react-hooks/preserve-manual-memoization 회피).
  - **`components/music/reading-complete-dialog.tsx` 수정** — handleOpenRecord 카나리 분기:
    - V2 + 진행 중 세션 발견 → `openRecordEnd(activeSession.id, { book })` 자동 진입 (사용자가 음악 종료 후 곧장 종료 페이지 입력)
    - V2 + 세션 없음 → `openRecordStart({ book, targetSeconds: elapsedSeconds })` (음악 시간 prefill)
    - Legacy → 기존 `openStampWithTimer`
  - **`note-creation-flow.tsx`·`/notes/new` 보존** — 데스크톱 큰 폼 흐름은 상세기록의 데스크톱 진입로 역할로 그대로 두고, Phase 6/7에서 추가 검토.
  - **legacy 액션 4종에 `@deprecated` JSDoc** 추가 (시그니처·동작은 변경 없음 — Phase 6에서 thin wrapper로 축소):
    - `app/actions/progress.ts::createProgressLog`
    - `app/actions/progress.ts::saveReadingSession`
    - `app/actions/progress.ts::createReadingStamp`
    - `app/actions/notes.ts::createQuickNote`
  - **검증**: `npm run type-check` ✅, vitest 본 개편 누적 20/20 ✅, 신규 lint 깨끗 (사전 회귀 isLoading·toast·setIsSaving·setMounted는 본 phase 무관)
  - **카나리 운영 가이드** (실행 시):
    - `.env.local`에 `NEXT_PUBLIC_RECORD_V2=1` → 내부 사용자만 활성화
    - Vercel env에 production 분기 설정 → 신규 가입자 10% 또는 user-id 해시 분배
    - 1주 안정 + photo/progress notes 신규 0건 확인 후 Phase 6에서 토글 제거 + legacy 호출처 정리
- **인사이트:** 토글 기반 점진 이행 — 코드 두 갈래 임시 보존 비용보다 즉시 롤백 가능성·A/B 비교의 가치가 큼. handleOpenRecord에서 `activeSession` 유무로 start/end를 자동 분기하여 음악 타이머 사용자가 별도 학습 비용 없이 새 모델로 흡수됨. 음악 자동 startReadingSession은 침습적이라 본 phase에서 보류 (선택적 enhancement). 다음은 Phase 6 (호환 정리·legacy 차단·orphan 정리 마이그).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 4 완료 (진행 중 인디케이터)
- **카테고리:** #구현 #UI #네비게이션
- **중요도:** P0
- **지시:** "검증 확인이후 다음단계 진행"
- **맥락:** Phase 3 RecordSheet의 sheet 진입점이 mounted된 상태에서, 진행 중 세션을 시각적으로 노출하는 인디케이터를 도입. 데스크톱은 헤더 inline pill, 모바일은 FAB 슬롯 in-place 변형으로 좌표 충돌 0 (D5).
- **결과:**
  - **`components/layout/active-session-indicator.tsx` 신설** — useReadingSession 구독, 비로그인/세션 없으면 null 반환. 클릭 시 useRecordSheetStore.openEnd로 책 정보 자동 prefill하여 end-step 진입.
  - **`components/layout/mobile-nav.tsx` 수정** — FAB(item.isFab) 슬롯에 진행 중 세션이 있을 때 RecordActivePill(fab variant)로 변형 (시간 + 책 표지). `-mt-3` 마진으로 기존 + 버튼 위치 정확히 보존. 진행 중 아닐 때는 기존 + 기록 버튼 그대로.
  - **`components/layout/header.tsx` 수정** — 우측 메뉴 맨 앞 `<div className="hidden sm:inline-flex">`에 ActiveSessionIndicator(pill variant) 배치. 모바일에서는 mobile-nav FAB가 책임지므로 hidden, sm 이상에서 inline-flex.
  - **`app/(main)/layout.tsx` 수정** — RecordSheet 컴포넌트를 layout에 mount (StampCaptureSheet 옆). 모든 페이지에서 useRecordSheetStore.openX() 가능.
  - **D5 좌표 충돌 검증**: MusicMiniPlayer는 `fixed bottom-14 sm:bottom-16 lg:bottom-0` (화면 하단 고정). 인디케이터는 mobile-nav FAB의 in-place 자리(bottom-0 nav 안), 데스크톱 헤더 우측(top-0 안). 두 컴포넌트 모두 fixed 영역이지만 좌표·z-index 모두 분리.
  - **`npm run type-check` 통과**, 본 phase 신규 파일 lint 깨끗
  - **vitest 본 개편 누적 20/20 통과** (Phase 4는 컴포넌트 wiring만 — 추가 단위 테스트 불필요)
  - **사전 회귀 식별** (본 phase 무관, 별도 PR 권장):
    - `header.tsx:43` setMounted in effect (테마 hydration mismatch 방지 패턴, 기존 코드)
    - `mobile-nav.tsx:59` useAuth 구조 분해의 `isLoading` 미사용 (기존 코드)
- **인사이트:** mobile-nav의 FAB 슬롯을 "변형 가능한 자리"로 재정의하여, 기존 + 버튼 / 진행 중 인디케이터가 같은 좌표를 공유 — 추가 fixed 위젯 없이 시각 충돌 0. 데스크톱 헤더 pill은 sm:inline-flex로 lazy 노출하여 모바일 nav와 책임 분리. 다음은 Phase 5 (진입점 통합 + 음악 reading-complete-dialog 자동 진입 + legacy deprecation 라벨).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 3 완료 (RecordSheet 통합 시트 UI)
- **카테고리:** #구현 #UI #리팩토링
- **중요도:** P0
- **지시:** "Phase 3 진행"
- **맥락:** Phase 2 액션·훅 위에 사용자가 보는 첫 UI. 세션 시작 → 진행 중 → 종료 → 상세 흐름을 단일 시트의 step 라우터로 통합. Phase 5 진입점 통합 전까지는 노출 없음 (안전).
- **결과:**
  - **`hooks/use-record-sheet.ts` 신설** — Zustand `useRecordSheetStore` (start/end/detail 모드 + targetSessionId + prefill). useRecordSheet 훅이 start 시 최근 책 자동 prefill, end 시 진행 중 세션의 책 자동 사용
  - **`components/records/` 7개 컴포넌트 신설** (총 ~700줄)
    - `record-sheet.tsx` — 모드 라우터. start 모드 진입 시 진행 중 세션 발견하면 end로 자동 전환 (D2)
    - `record-start-step.tsx` — 책·시작 페이지 (getLastEndPage prefill)·시간 프리셋 4종(15/25/45/무제한). startReadingSession + 멱등키(generateClientSessionId) + BroadcastChannel
    - `record-end-step.tsx` — 끝 페이지·간단 메모(≤200)·북마크 토글·사진 strip(≤5). 저장 / "저장+상세 기록" 두 갈래 + 취소(30초 임계)
    - `record-detail-step.tsx` — 종류 라디오(quote/memo/transcription) + 본문(quote≤5000, memo≤10000) + 페이지(선택). addNoteToSession (sessionId NULL = 자유 상세 D3)
    - `record-bookmark-toggle.tsx` — 토글 + bookmark_text(≤200) + bookmark_page (D1)
    - `record-photo-strip.tsx` — 가로 스크롤 ≤5장, 카메라/앨범 분리, 첫 장 "대표" 배지, 다중 업로드, 진행 인디케이터
    - `record-active-pill.tsx` — Phase 4 인디케이터의 표시 위젯 (FAB·pill 두 variant). MM:SS 또는 H:MM:SS 자동, accent → warning → destructive 색 분기 hook
  - **`__tests__/hooks/use-record-sheet.test.ts` 6개 테스트 통과** — 초기상태, openStart prefill, openEnd, openDetail D3 자유 상세, close 보존, reset 초기화
  - **`npm run type-check` 통과** (회귀 0)
  - **vitest 본 개편 누적 20/20 통과** (sessions 14 + record-sheet 6)
  - **재사용**: useReadingSession, broadcastSessionStarted/Ended/Cancelled, generateClientSessionId, getContinueReadingBooks, getLastEndPage, smartCompressImage, /api/upload, sonner toast, shadcn Sheet/Dialog/Input/Switch/Textarea
  - **next/image** 사용 — record-photo-strip 썸네일·record-active-pill 표지 모두 next/image fill 패턴
- **인사이트:** start 모드에서 진행 중 세션 감지 시 end 모드로 자동 전환하는 안전망(D2 한 명 한 세션) — DB unique 인덱스가 백업하지만 사용자 입력 흐름 마찰을 시트 레벨에서 한 번 더 차단. RecordEndStep의 "저장 / 저장+상세 기록" 두 액션 분기로 토스트 액션을 시트 안에 흡수 — 토스트 누적·미스 클릭 회피. 다음은 Phase 4 (active-session-indicator + mobile-nav/header 결합).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 2 완료 (세션 액션 + 라이브 훅)
- **카테고리:** #구현 #리팩토링 #테스트
- **중요도:** P0
- **지시:** "이어서 Phase 2 진행"
- **맥락:** Phase 1 DB 적용 완료 후, 새 진입점 액션과 훅 신설. 기존 progress.ts/notes.ts 함수는 그대로 두고(Phase 5 위임), 신규 코드만 추가하여 회귀 0.
- **결과:**
  - **`app/actions/sessions.ts` 신설** (476줄, 6 함수)
    - `startReadingSession` — 멱등(client_session_id), READTREE_BOOK_ID 폴백, getLastEndPage 자동승계, unique 위반(D2) 명확한 에러 매핑
    - `endReadingSession` — status='completed' 전환, image_urls(≤5), bookmark_text/page(D1), DB 트리거가 image_url 자동 미러링·promoted_at 자동, earnPoints 1회(D4)
    - `getActiveSession` — RLS + status='in_progress' 단일 조회, 책 join 포함
    - `cancelActiveSession` — 30초 미만 DELETE, 이상 abandoned
    - `addNoteToSession` — sessionId NULL = 자유 상세(D3), detail_kind 라벨링, 포인트 별도 적립 없음
    - `reapOrphanSessions` — 12h orphan abandoned (Phase 6 cron 후보)
  - **`hooks/use-reading-session.ts` 신설** (174줄)
    - Zustand `useReadingSessionStore` (optimistic 캐시 + pendingClientSessionId)
    - 폴링(30s) + visibilitychange + BroadcastChannel("readtree-session") 다중탭 동기
    - `useElapsedSeconds` — started_at 기준 1초 tick 라이브 카운트
    - `broadcastSessionStarted/Ended/Cancelled`, `generateClientSessionId` UUIDv4
  - **vitest 14/14 통과** (`__tests__/actions/sessions.test.ts`)
    - 멱등성: 동일 client_session_id → isResumed=true, INSERT 미호출
    - D2 unique 위반: "이미 진행 중인 기록" 에러 매핑
    - 입력 검증 4종: session_id UUID, end_page≥0, memo≤200, image_urls≤5
    - earnPoints 1회 적립(D4), 이미 종료된 세션 거부
    - cancel: 30초 미만 DELETE, 이상 abandoned, 이미 완료 noop
    - addNoteToSession: 자유 상세(D3) 허용, sessionId 지정 시 소유 확인
  - **`vitest.config.ts` exclude 추가** (.claude/worktrees, doc/qa-inspection — Playwright e2e)
  - **`npm run type-check` 통과** (회귀 0)
  - **vi.hoisted 패턴 검증**: sessions.ts가 server action으로 즉시 import되어 mock factory가 동기 평가됨 → vi.hoisted로 mockSupabase·mockEarnPoints를 호이스트 시점에 초기화하여 TDZ 회피 (기존 `auth.test.ts`·`points.test.ts`의 사전 회귀와 동일 원인 — 향후 별도 PR로 정리 가능)
- **인사이트:** SWR 미설치 → React 내장 setInterval+useState로 동등 효과 구현 (의존성 추가 없음). DB 트리거(M2)가 image_urls→image_url 미러링 + promoted_at 자동 set을 책임지므로 endReadingSession은 image_urls만 set하면 끝 — 코드 분기 1단 절약. 다음은 Phase 3 (RecordSheet 7개 컴포넌트 + use-record-sheet 훅).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 1 완료 (DB 마이그레이션 4종 + 타입)
- **카테고리:** #구현 #리팩토링 #DB
- **중요도:** P0
- **지시:** "Phase 1 DB 적용 진행 (Supabase MCP)"
- **맥락:** Phase 0 기획 문서 합의 후, 데이터 모델부터 점진 도입. 모든 마이그는 idempotent + 컬럼 단독 DROP으로 즉시 롤백 가능.
- **결과:**
  - **마이그 4종 prod 적용** (Supabase MCP `apply_migration`):
    - M1 `reading_logs_add_session_columns` — status, bookmark_text/page, image_urls(JSONB), client_session_id, app_version + CHECK 3종 + 부분 인덱스 3종(in_progress 가속, 사용자당 1세션 unique D2 강제, client_session_id 멱등)
    - M2 `reading_logs_image_urls_sync_trigger` — image_urls[0] ↔ image_url 양방향 동기 + image_url 첫 set 시 promoted_at 자동 (DB 안전망)
    - M3 `notes_add_reading_log_link` — reading_log_id FK(SET NULL) + detail_kind CHECK + 인덱스
    - M4 `backfill_status_and_image_urls` — 기존 행 status='completed', image_urls 미러링 백필
  - **검증 SQL 9개 모두 통과**: 컬럼 존재(6+2), 인덱스(3+1), 트리거 동작, status NULL 0, invalid 0, image_urls unsynced 0, in_progress 중복 0
  - **트리거 실동작 PL/pgSQL 테스트** 통과 (image_urls→image_url 미러링, promoted_at 자동 set 양쪽 검증)
  - **types/database.ts 자동 재생성** (Supabase MCP `generate_typescript_types`)
  - **types/progress.ts·note.ts** 확장: SessionStatus, DetailKind, StartSessionInput, EndSessionInput, AddDetailInput, ReadingLogActive, ReadingLog/Note 신규 컬럼 6+2개
  - **app/actions/progress.ts:773** 매핑 호환 fallback 추가
  - **`npm run type-check` 통과** (회귀 0)
  - **기존 백업** types/database.ts.bak 보존
- **인사이트:** ADD COLUMN IF NOT EXISTS + DEFAULT 'completed' 조합으로 기존 코드(SELECT *)는 신규 컬럼을 자동 흡수. 트리거를 DB에 두면 image_urls만 set해도 /stamps의 `image_url IS NOT NULL` 쿼리가 그대로 동작 — 코드 분기 1단 절약. 다음은 Phase 2 (sessions.ts + use-reading-session.ts 신설).

---

### 2026-05-04 — 기록 기능 전면 개편 Phase 0 완료 (단계별 기획 문서 13종)
- **카테고리:** #기획 #리팩토링 #UX
- **중요도:** P0
- **지시:** "기록 기능을 전면 개편 — 시작/종료 세션 모델 + 2분류(기록·상세기록)로 통합. 단계별 기획 문서 먼저 만들고 고도화 진행"
- **맥락:** 현재 기록은 흩어져 있다 — `notes`(quote/photo/memo/transcription/progress 5종) + `reading_logs` + 음악 자동 저장 + Quick Capture + Free Notes. 사용자가 무엇을 누를지 매번 결정. Phase 1·2 스탬프 통합으로 reading_logs는 정리됐지만 노트 분류·진입점은 여전히 5+ 갈래.
- **결과:**
  - **세션 기반 모델 확정**: 모든 기록은 `reading_logs` 한 행에서 시작·종료. status = `in_progress` / `completed` / `abandoned`
  - **분류 단순화 2종**: 기록(시간·페이지·메모·북마크·사진들) + 상세기록(필사·구절·긴 생각)
  - **결정 5개 (ADR)**: D1 북마크=한 줄 메모 / D2 사용자당 1세션 / D3 자유 상세 허용 / D4 종료 1회 적립 / D5 음악·세션 인디케이터 별도
  - **스탬프 정의 불변**: `image_url IS NOT NULL AND promoted_at IS NOT NULL` 그대로 — `/stamps` 영향 0
  - **기획 문서 13개 작성** (`doc/update/기록기획/`)
    - `00-master.md` 인덱스·Phase 게이트
    - `01-data-model.md` 컬럼·트리거·ERD
    - `02-migration.md` 마이그레이션 5종 + dry-run + 롤백
    - `03-ux-flow.md` 시작→진행→종료 플로우 + 인디케이터 규칙
    - `04-rollout.md` Phase별 PR + 카나리
    - `05-decisions.md` ADR D1~D5
    - `06-rollback.md` 롤백 카탈로그
    - `phases/phase-1~7-*.md` 7개 phase 체크리스트
  - **plan 파일 보존**: `~/.claude/plans/foamy-frolicking-lightning.md`
  - **단계 7개 정의**: Phase 1 데이터모델 → 2 액션 → 3 시트UI → 4 인디케이터 → 5 진입점 통합 → 6 legacy 차단 → 7 검증·텔레메트리
- **인사이트:** Phase 1·2 스탬프 통합으로 데이터 절반은 정리됐지만 사용자에게 노출되는 진입점·UI 분류가 여전히 다층 구조. "DB 모델"을 단순화하는 것보다 "사용자가 누르는 버튼의 종류"를 단순화하는 것이 본 개편의 핵심 가치. 2분류 = 기록(세션)+상세(텍스트)로 압축하면 향후 음성·동영상 등 모달리티 확장도 같은 축에 흡수 가능. 카나리 토글(`NEXT_PUBLIC_RECORD_V2`)로 Phase 5 진입점 통합 시 안전장치 확보.

---
