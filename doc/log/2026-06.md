# 2026-06 Prompt Log — Habitree ReadingTree

---

### 2026-06-10 — 스탬프 재통합 + 내 기록 통합 뷰 (기록 기획 12, A→B→C→D 일괄)
- **카테고리:** #기능 #리팩터 #정합성 #문서화
- **중요도:** P1
- **지시:** "스탬프 적용 방식 + 기존 기록/시간 데이터 결합 + 별도 스탬프가 아닌 시간기록 이후 스탬프 관리 + 전체 분석해 묶어 정리 → HTML 기획" → 검토 후 "전체 내용 문제없이 기획하고 전부 진행 반영".
- **갭 진단:** 스탬프 = 사진 첨부된 `reading_logs` 행(이미 구현). 그러나 2026-04-30 통합 기획은 **legacy `StampCaptureSheet`** 기반이었고, 이후 도입된 세션모델(`RecordSheet`+`sessions.ts`) 위에서의 **스탬프 재통합이 로드맵(묶음1~5)에 누락**. 새 시트에 attach(사후 첨부) 모드 없음, 종료 토스트 "사진 추가" 미연결(`stamp-toast.onAddPhoto` 슬롯 빈 채), `getRecentRecordsForAttach` 호출처 0건.
- **기획 산출물:** `doc/update/기록기획/12-stamp-reintegration.md` + `doc/update/스탬프재통합_기록통합_기획_2026-06-10.html`(9섹션: 갭·4축 멘탈모델·단일흐름·재통합·통합뷰·진입점 매트릭스·정리맵·실행계획·결정 정합성). 방향 확정(AskUserQuestion 3문): 스탬프 중심+정리맵 / legacy 점진(패리티→ON→제거) / 통합뷰=`reading-speed-detail` 확장.
- **결과 (A→D):**
  - **A 시간통계 통합:** `lib/reading/time-stats.ts::summarizeReadingTime`(순수) 신설 → `getUserReadingTimeStats`·`getReadingTimeStats`(progress.ts) 합산 로직 위임(중복 제거, 동작 보존). DB 접근은 액션에 유지(레이어 규칙).
  - **B 스탬프 재통합:** `use-record-sheet`에 `"attach"` 모드+`openAttach(logId)` 추가. `components/records/record-attach-step.tsx` 신설(사진→`attachStampToLog`/페이지·메모→`updateReadingLogEntry`). `record-sheet` attach 라우팅(`key={targetLogId}` 재마운트). `record-end-step` 종료 토스트 — 사진 없이 저장 시 "사진 추가"→openAttach, 사진 있으면 "공유". `reading-time-tab` openEditSheet 카나리 분기(ON→RecordSheet attach, OFF→legacy).
  - **C 카나리 ON 전환:** `lib/feature-flags::isRecordV2Enabled` 기본값 **ON**(미지정/빈값→true), `NEXT_PUBLIC_RECORD_V2=0`만 OFF(킬 스위치). **legacy(StampCaptureSheet/createReadingStamp) 물리 삭제는 ON 1주 검증 후 별도 PR로 보류**(점진 방침). 본 시점 legacy는 dormant(미오픈).
  - **D 내 기록 통합 뷰:** `PaceSession`에 `memo·imageUrl·imageUrls·promotedAt` 추가, `getPaceSessions` select·매핑 확장. `reading-speed-detail` — `[전체/시간만/스탬프]` 세그먼트 필터, 사진 썸네일+스탬프 배지, "사진 추가" 칩→openAttach, 메모 인라인 편집(편집폼 memo 필드), `Lightbox` 재사용. `attachStampToLog`에 `/profile/reading-speed` revalidate 추가.
- **결정 정합성:** D2(in_progress 1개)·D4(포인트 종료 1회, attach 추가적립 없음)·DEC-6(여정 편입)·스탬프 정의(image_url≠NULL AND promoted_at≠NULL) 불변. `/stamps`·`/share`·공유 컴포넌트 전부 유지.
- **검증:** `tsc --noEmit` ✅ · ESLint(변경 10파일) ✅(0 error). `next build` 진행.
- **인사이트:** "스탬프=세션의 한 상태" 멘탈모델로 재통합 = 모든 경로를 `endReadingSession`+`attachStampToLog` 두 경로로 수렴. 나머지 정리(A3/A5/C7/C8/C9/B1/C3)는 기존 묶음1~5에 이미 설계돼 있어 중복 없이 연결만.
- **브랜치:** `feat/record-time-consolidation`. **문서:** `12-stamp-reintegration.md` 체크리스트.

---

### 2026-06-08 — 독서 속도(페이지당 시간) 측정 — 전체 평균 확장 (C2+)
- **카테고리:** #기능 #리팩터 #통계
- **중요도:** P1
- **지시:** "독서기록을 토대로 책별 페이지당 독서속도를 누적하고, 나의 전체 페이지당 속도를 측정 — 복잡하지 않게, 데이터를 자연스럽게 누적·관리하며 기존 기능·데이터를 최대한 활용해 기획→구현"
- **맥락:** 세션별 페이스는 `reading_logs.pace_seconds_per_page`(STORED)로 이미 누적되고, **책별 평균은 C7 `ReadingPacePanel`로 이미 표시 중**. 즉 원시 데이터는 행 단위로 자연 누적되어 있어 실제 공백은 **"나의 전체 페이지당 평균 속도"**(책 무관 통합 지표) 하나뿐. 저장 방식·노출 위치는 AskUserQuestion 2문으로 확정 — **실시간 집계만**(새 테이블/컬럼·마이그 없음, 드리프트 없음) + **3곳 노출**(통계·책상세·프로필).
- **결과:**
  - **공용 유틸 SSOT** — `lib/reading/pace.ts` 신설: `computePace`(적격 세션 필터 + Σ시간/Σ페이지 **가중 평균**, NaN 방지 null 반환), `formatPacePerPage`(reading-pace-panel에서 이동), `estimateRemainingSeconds`.
  - **서버 액션 확장** — `getUserReadingTimeStats`(progress.ts) select에 `start_page·end_page` 추가 → `pacePerPageSeconds·totalPagesRead` 산출. `UserReadingTimeStats` 타입·샘플 데이터(sample.ts)·홈히어로 폴백 동기화.
  - **노출 ① 통계** — `stats-content.tsx` "페이지당 평균" 요약 카드(Gauge, pace null이면 미표시).
  - **노출 ② 책상세** — `ReadingPacePanel`을 공용 유틸로 리팩터(동작 보존) + `overallPaceSeconds` prop으로 "내 평균보다 N% 빠르게/천천히/비슷"(±10% 임계) 비교 줄. `reading-time-tab`에서 전체 pace 병렬 로드.
  - **노출 ③ 프로필** — `components/profile/reading-speed-card.tsx` 서버 컴포넌트 신설(적격 데이터 없으면 미렌더).
  - i18n(ko/en) 키 2종 + `__tests__/lib/pace.test.ts` 8케이스(역순/널/0초 제외·가중값·빈입력 null).
- **검증:** vitest 14/14(pace 8 + metrics 6) ✅ · `tsc --noEmit` ✅.
- **인사이트:** 요청의 대부분이 이미 구현돼 있던 케이스 — 탐색으로 "C7=책별 이미 존재, 공백=전체 평균" 격차를 먼저 좁히니 신규 작업이 가중 집계 유틸 1개 + 노출 3곳으로 수렴. "누적·관리"는 새 저장소가 아니라 **기존 행 집계**로 충족(드리프트 0).
- **커밋:** `29a72f2` 푸시 완료. **브랜치:** `feat/record-time-consolidation`. **문서:** `10-implementation-spec.md` C2+ 행 추가.

---

### 2026-06-04 — 기록·시간 전수 분석 → 통합 기획(v2) + P0 착수 (대규모, 진행 중)
- **카테고리:** #기획 #리팩토링 #정합성 #문서화
- **중요도:** P0
- **지시:** "기록과 시간 전 기능을 취합·분석 → 합칠/정리할/새로 만들 부분 정리 → 기획·HTML 문서화 → 의사결정" → 이후 "독서 3축(독서시간/읽기진행률/여정)·독서법(읽기+쓰기+출력)·기록시간vs독서시간·내기록 독서시간 반영" → "추천안으로 진행하며 문서 남기며 계속"
- **분석:** Explore 4종 병렬 전수조사. 핵심 발견 — ① 3축(⏱`reading_duration_seconds`/📊`current_page÷total_pages`/🧭`reading-journey.tsx`)이 서로 다른 테이블·계산으로 분산 ② 진행률 수정→`notes(type=progress)`→여정 점(편입 기준 부재) ③ `created_at`(기록시간)이 `reading_duration_seconds`(독서시간)를 가림 ④ 스트릭 DB값 vs 계산값 불일치 ⑤ KST 헬퍼·duration 포맷 중복.
- **기획 산출물:**
  - `doc/update/기록시간_통합기획_2026-06-04.html` (v2, 12섹션) — 3축 모델·독서법·A/B/C 분류·우선순위 매트릭스·의사결정 DEC-1~8(권장안 표시).
  - `doc/update/기록기획/10-implementation-spec.md` — 권장안 확정 + P0~P2 작업분해 + 구현 노트.
- **P0 구현(동작 보존 리팩토링):**
  - **A1** `lib/utils/timezone.ts` 신설(+`getKSTYearMonth`/`isFutureKSTMonth`) → 앱 실사용 KST **11파일** 위임(`stats.ts`·`compute.ts`·`books-list.ts`·`generate.ts`·cron route·`stats/page`·`recap/[yearMonth]/page`·`home-hero-wrapper`·`tertiary-zone-wrapper`·`recap-month-switcher`·`notes.ts`). 잔여는 sample/demo 데이터뿐.
  - **A2** `lib/utils/duration.ts` 신설(`formatDuration`/`formatClock`/`formatTimeRange`) → `reading-time-tab`·`music-mini-player`·`track-list-sheet`·`lib/recap/text.ts` 4곳 위임. `formatReadingTime`은 `{rounding:"round"}`로 출력 숫자 보존.
  - **B3** `lib/utils/streak.ts` 신설(`computeCurrentStreak`/`computeMaxStreak`) → stats·recap 스트릭 **계산식 단일화**(불일치 해소).
- **검증:** `npx tsc --noEmit` ✅ (에러 0). 중복 코드 ~60줄 제거.
- **C6(내 기록 독서시간) 추가 구현:** `getTimeline` select에 `reading_logs(reading_duration_seconds)` 임베드(FK `notes_reading_log_id_fkey`) + `note-card` 메타라인 ⏱배지(`formatDuration`). "읽은 시각" vs "읽은 시간" 구분. 검증: tsc ✅ · FK·조인 실값(1337초) MCP 확인 · dev `/notes` 200(임베드 정상).
- **다음 단계 일괄 설계 문서화:** `doc/update/기록기획/11-next-steps-design.md` 신설 — 남은 로드맵(A5·A3·B3-2·C7·C8·C9·B1 + 후순위)을 의존성 순서로 설계(목표·근거·파일/시그니처·SQL·리스크·검증·체크리스트). MCP로 확정: **C9는 `notes_detail_kind_check` 제약 때문에 마이그레이션 필수**, **B1은 `note_type` ENUM이라 앱 가드만**. `10-implementation-spec.md` P1/P2 표·참조 갱신.
- **묶음1 구현(A5→A3→B3-2):**
  - **A5** `types/reading-metrics.ts`(3축 타입) + `lib/reading/progress.ts::computeProgressPercent` 신설 → 진행률 중복 5곳(reading.ts×2·books-list·reading-progress×3) 위임(동작 보존, fallback null/0 구분).
  - **A3** `lib/reading/metrics.ts::computeReadingMetrics` 순수 코어 + `__tests__/lib/reading-metrics.test.ts`(독립 레퍼런스 동등성 6/6) → `recap/compute.ts` 일반 메트릭 위임(하이라이트는 유지). 라이브·스냅샷 단일 출처.
  - **B3-2** `getWeeklyProgress`의 세 번째 스트릭 루프를 `computeCurrentStreak`로 위임 → 홈·통계·결산 스트릭 단일 출처. DB `current_streak`은 게이미피케이션 미션 전용 유지(적립 규칙 불변).
  - 검증: `tsc` ✅ · vitest(metrics) 6/6 ✅ · dev `/stats`·`/recap/2026-06` 200(에러 없음) ✅. (기존 auth/points 테스트 2건 실패는 무관한 vi.mock 호이스팅 이슈.)
- **묶음2 구현(C9·C7):**
  - **C9 출력(독후감/리뷰):** 마이그레이션 `migration-202606041200__notes__add_detail_kind_review.sql`(CHECK에 'review' 추가, MCP `apply_migration` 적용·확인). `DetailKind += review`, `addNoteToSession`이 review→type 'memo' 매핑(note_type ENUM 미확장), RecordSheet detail-step에 "독후감·리뷰" 옵션, note-card에 detail_kind='review' 라벨/아이콘. 검증: DB insert(type=memo+detail_kind=review) 트랜잭션 검증 ✅ · tsc ✅ · dev `/notes` 200 ✅.
  - **C7 페이지별 독서 페이스:** `components/books/reading-pace-panel.tsx` 신설(구간 start_page~end_page+시간 → 페이지당 평균·읽은 분량·남은 예상시간) → `reading-time-tab` 통계 카드 아래 통합. `formatDuration` 재사용. tsc ✅.
- **실제 화면 Playwright 캡처 + C6 버그 수정:** 변경 화면을 dev(게스트)에서 캡처(`doc/update/기록시간_실제화면_캡처_2026-06-04.html` + `screenshots/`). 캡처 중 **C6 배지가 `/notes`에 안 뜨는 버그 발견** — C6를 `getTimeline`에 넣었으나 `/notes`는 실제로 `searchNotes`(로그인)·`getSampleAllNotes`(게스트)를 사용. 두 함수에 `reading_logs(reading_duration_seconds)` 조인 + 매핑 추가해 **수정**(임시 데모 기록으로 캡처 검증 후 삭제). C8 3축 패널·C9 독후감 라벨도 실제 렌더 확인. (C7 페이스·C8 시간값은 게스트 인증 제약으로 로그인 전용.)
- **묶음5 마무리(C1·C3·C4·B5·B2 재평가):**
  - **C1 타이머 목표 UI:** 코드 확인 결과 **이미 완전 구현**(`record-start-step` 프리셋 15/25/45/무제한 → target_seconds, `record-timer-complete-dialog` elapsed≥target 감지, `app/(main)/layout.tsx` 전역 마운트). 재구현 불필요 — 설계의 "불완전" 평가 정정.
  - **C2 페이스:** C7(`reading-pace-panel`)로 이미 노출.
  - **C3 캘린더 SSOT 재평가→불필요:** activity-calendar(타입별 30일 히트맵)·monthly-book-calendar(책별 월간)는 서로 다른 facet, KST는 A1으로 이미 공유 → 억지 통합 가치 낮음.
  - **C4 음악-세션 동기화:** `attachMusicToSession` 존재하나 자동 재생/정지는 UX 민감 신규 기능 → 향후 별도.
  - **B5 DATA_MODEL 현행화:** `reading_logs`(4.20) 섹션이 v1.0/Phase 컬럼 전부 누락 상태였음 → 세션·페이지(📊)·시간(⏱)·스탬프·음악 전 컬럼 + 타임스탬프 계층("적은 시각 created_at" vs "읽은 시간 reading_duration_seconds") 정리.
  - **B2 재평가:** createReadingStamp는 stamp-capture-sheet에서 **활성** → 유지. createProgressLog는 **호출처 0(고아)** → 제거 후보.
- **묶음5 구현(DEC-6 — 진행률→여정 편입 규칙):** `upsertDailyProgressNote`(notes.ts) 신설 — 메모 없는 단순 진행은 같은 날(KST) 같은 책의 메모없는(content IS NULL) progress 노트를 page만 갱신하여 **일 1점 집약**, 없으면 생성. `reading-progress.tsx` 메모없음 저장 경로에서 **전진(page 증가)만 기록**(후퇴/정정 제외) + 이 액션 호출. 메모 동반 저장은 항상 별도 생성(불변). 검증: tsc ✅ · MCP 실데이터 — 메모없음 156 vs 메모 29건 구분 정확, 같은날 중복 17건(156→139 distinct)으로 집약 효과 확인. 최악의 경우=현행(회귀 없음).
- **묶음4 구현(B6·A4·B1 재조정):**
  - **B6 orphan cron:** `app/api/cron/reap-orphan-sessions/route.ts` 신설(admin 클라이언트로 전체 사용자의 12h+ in_progress→abandoned 일괄 정리 + record_abandoned 텔레메트리) + `vercel.json` cron(`0 15 * * *`). reapOrphanSessions는 RLS로 현재 사용자만 처리하므로 cron 전용 admin 경로 추가. 검증: dev `/api/cron/reap-orphan-sessions` → 200 `{closed:0}`(orphan 0건 확인).
  - **A4 진입점 정리:** 고아 컴포넌트 `components/books/progress-record-sheet.tsx`(importer 0) 삭제. RecordSheet가 단일 진입점.
  - **B1 데이터 기반 재조정:** MCP 카운트 — type=photo 최근30일 **0건**(사망), type=progress 최근30일 **15건**(활성, 여정 데이터 소스). v1.0 "photo+progress 차단" 전제가 현실과 달라, **progress는 차단 안 함**. `createNote` deprecation warn을 photo에만 한정(progress 오경고 제거). 검증: tsc.
- **묶음3 구현(C8 — 3축 통합 뷰):** `components/books/reading-overview-panel.tsx` 신설 — ⏱독서시간(`getReadingTimeStats`)·📊진행률(`computeProgressPercent`+바)·🧭여정(회독·기록) 요약 스트립을 `book-notes-tabs` 탭 위에 통합(요약→탭 드릴다운). A5 유틸/A2 포맷 재사용으로 타 화면과 숫자 정합. 검증: tsc ✅ · 책 상세 200 · Playwright 렌더 확인(진행률 100%·p.372/372, 여정 1회독·22기록, 게스트 시간 "—" 폴백). 게스트 샘플 조회 시 `getReadingTimeStats` throw로 인한 500은 기존 `reading-time-tab`과 동일한 기존 동작이며 `.catch`로 폴백(로그인 사용자 정상).
- **보류/잔여:** B1(레거시 photo 차단)은 `notes.ts:224` 사진 업로드가 아직 `type="photo"` 활성 생성 → 경로 이관 후로 보류. B3-2(gamification `current_streak` 정합)·A3(집계 코어)·C7(페이지별 시간)·C8(3축 통합뷰)·C9(출력 review)는 후속 증분.
- **커밋:** `318fa12`(A1·A2·B3) 푸시 완료. C6는 후속 커밋 예정.
- **브랜치:** `feat/record-time-consolidation`

---

### 2026-06-01 — 월간 "읽은 책" 대시보드 (결산 확장)
- **카테고리:** #신규기능 #UX #결산
- **중요도:** P1
- **지시:** "리포트에 그달 읽은 책들을 대시보드에서 표지·내용과 함께 보이게, 총권수·가시적 권수, 우리 서비스에 어울리게 고급스럽게 페이지도 생성"
- **방향 확정(AskUserQuestion 4문):** ① 활동한 모든 책(완독 강조) ② 결산 미리보기 + 전용 페이지 ③ 인앱 전용(공개 공유는 집계만 유지) ④ "커버 책장 월(wall)" 히어로
- **결과:**
  - **데이터** — `app/actions/recap/books-list.ts` `getMonthlyBooksList(year,month)`: notes+reading_logs+완독 union을 bookId로 dedupe, per-book(표지·노트수·시간·페이지·발췌·진행률·완독여부) 집계, 완독 우선 정렬. KST 경계·게스트(admin+sample) 패턴 재사용.
  - **공유 유틸 추출** — `lib/recap/text.ts`(`extractQuoteText`·`formatReadingTime`) → compute.ts 중복 제거 + books-list 공유.
  - **컴포넌트** — `monthly-books-hero`(커버 책장 월: 표지 겹침 선반 + 큰 권수), `monthly-book-card`(표지 3:4 hover scale + 완독 리본 + 메타칩 + 발췌, `/books/{userBookId}` 링크), `monthly-books-showcase`(preview/full 변형, 빈 상태 EmptyState).
  - **전용 페이지** — `app/(main)/recap/[yearMonth]/page.tsx`(YYYY-MM 파싱·미래월 redirect, 헤더+월스위처+full 그리드) + `loading.tsx` 스켈레톤 + `recap-page-month-switcher`(router.push 래퍼).
  - **/stats 통합** — `recap-section.tsx`에 `initialBooks` + preview showcase(인앱 전용, RecapView 미수정), `stats/page.tsx` `getMonthlyBooksList` 병렬 호출.
  - 타입: `MonthlyBookItem`/`MonthlyBooksResult` (`recap/types.ts`).
- **검증:** `tsc --noEmit` ✅ · ESLint ✅ · `next build` ✅(`/recap/[yearMonth]` 라우트 컴파일 확인)
- **설계 노트:** 책 목록은 라이브 쿼리(스냅샷 미저장) — 공개 공유 페이지는 기존대로 집계만(스크린샷 비대·프라이버시). 결산 공유 파이프라인·크론·DB 스키마 변경 없음.

---

### 2026-06-01 — 월간 독서결산(Monthly Reading Recap) 신규 기능 (대규모)
- **카테고리:** #신규기능 #공유 #리텐션 #통계
- **중요도:** P0
- **지시:** "독서달력에서 한달의 독서내역을 취합·결산하는 메뉴 — 공유·관리, 사용자들이 자신의 한달 독서를 공유하고 가치있는 내용을 공유. 카페·블로그 등 벤치마킹하여 사용자가 우리 서비스를 쓰고싶게 기획·적용"
- **방향 확정(AskUserQuestion 4문):** ① 통계 페이지 통합 ② 외부 공유 우선(카카오/X/링크/이미지 + 공개페이지 CTA) ③ 월말 자동생성+알림 ④ 차별화 4요소 전부(AI 페르소나·전월/목표 대비·하이라이트 인용·뱃지)
- **맥락:** 스탬프 공유 파이프라인(다이얼로그·OG·공개페이지·share-channels)과 알림·페르소나(Gemini)·골·포인트/스트릭 인프라가 이미 완비 → 신규 구축은 스냅샷 테이블·집계·월말 크론에 집중. `ai_generated_reports`의 share_id 토큰 스키마를 선례로 채택.
- **결과:**
  - **DB** — `doc/database/migration-202606011000__monthly_recaps__create_table.sql`. 스냅샷 테이블(stats/highlights JSONB, share_id 토큰, share_version, ai_caption) + RLS 4정책(SELECT=공개 OR 본인) + 인덱스 + updated_at 트리거.
  - **집계** — `app/actions/recap/{types,compute}.ts`. `computeRecapForUser(supabase, userId, y, m)` — 클라이언트 명시형(인앱·게스트·크론 공용). KST 경계 공식 재사용. 4차별화: 규칙기반 페르소나 타이틀(시간대×스타일×볼륨), 전월/목표(getGoalProgress 인라인) 대비, 베스트 인용, 뱃지(스트릭·완독·기록·시간).
  - **생성/공유 액션** — `app/actions/recap/{generate,share}.ts`. `generateMonthlyRecap`(UPSERT, 변경 없으면 write 생략으로 share_version 인플레 방지), `getRecapForView`(현재달 재스냅샷/과거달 동결/게스트 데모), `getRecapForShare`(anon-safe), `setRecapPublic`, `getRecapAiCaption`(Gemini 지연 생성·캐시).
  - **공유 UI** — `components/recap/{recap-share-card,recap-share-dialog}.tsx` + `hooks/use-recap-share.ts`(스탬프 포크, 1080×1350 캡처, 링크/이미지/카카오/X/native). `lazy-overlays.tsx` latch 마운트. `share-channels.ts`·`og/meta.ts`에 `"recap"` kind 추가.
  - **인앱 UI** — `components/recap/{recap-section,recap-view,recap-month-switcher}.tsx`. `/stats` 상단에 월 스위처 + 결산 섹션 통합. `?m=YYYY-MM` 딥링크(알림 진입점).
  - **공개 페이지+OG** — `app/share/recaps/[id]/{page,opengraph-image}.tsx`. share_id+is_public RLS, 읽기전용 RecapView + "나도 시작하기" CTA + ReferralTracker, 1200×630 OG(완독 표지 스택+페르소나+스탯+인용).
  - **월말 크론+알림** — `app/api/cron/generate-monthly-recap/route.ts` + `vercel.json`(매월 1일 00:00 UTC) + `CRON_SECRET`. 전월 활동 유저 수집 → 스냅샷 → `report_ready` 알림(`notification_prefs` 존중).
- **검증:** `tsc --noEmit` ✅ (에러 0), ESLint 클린 ✅, `next build` ✅ (신규 라우트 `/share/recaps/[id]`·OG·`/api/cron/*` 컴파일 확인)
- **후속(미적용):** 마이그레이션 프로덕션 DB 적용(수동), Vercel `CRON_SECRET` 환경변수 설정, 앱 내 발견 피드·그룹 결산·연간 결산은 향후 확장.
- **플랜 문서:** `~/.claude/plans/shiny-drifting-pinwheel.md`

---

### 2026-06-01 — 속도 우선 전체 점검 + 스탬프 공유 흐름 마감 (중규모)
- **카테고리:** #성능 #UX #스탬프
- **중요도:** P1
- **지시:** "현재 사용자의 속도개선과 편리성을 기준으로하여 전체적인 점검을 통해 개선할점을 확인해서 기획하고 업데이트 진행해줘" (방향: 속도 우선 / 스탬프 흐름 마감 / 중규모)
- **맥락:** Explore·Plan 에이전트로 핫패스 점검. 코드베이스는 이미 성숙(next/image, lazy-overlays 6종 지연 마운트, book-search 캐시+SWR, 홈 히어로 Suspense 스켈레톤, lib/cached.ts React cache() 디듀프). 광범위 리팩토링 대신 검증된 병목·체감속도·진행 중 스탬프 마감에 집중.
- **결과:**
  - **A1 ★ 홈 히어로 쿼리 1-웨이브 병합** — `components/dashboard/sections/home-hero-wrapper.tsx`. 직렬 2-웨이브(그룹1 7쿼리 await 후 그룹2 5쿼리)를 단일 `Promise.allSettled`(12개)로 병합. 두 그룹 간 데이터 의존성 없음 확인. 최다 방문 페이지에서 DB 라운드트립 한 웨이브 제거. (popularBooks만 continueReadingBooks 의존이라 후속 웨이브 유지)
  - **A3 어드민 통계 병렬화** — `app/actions/admin/stats.ts`. `getUserGrowthData` 6회 직렬 for-루프 → `Promise.all`, `getRecentSystemActivity` 2쿼리 → `Promise.all`.
  - **B1 reading-time-tab 스켈레톤** — `components/books/reading-time-tab.tsx`. 중앙 스피너 → 통계 3-그리드 + 로그 리스트 스켈레톤(CLS 제거).
  - **B2 스탬프 공유 다이얼로그 스켈레톤** — `components/stamps/stamp-share-dialog.tsx`. 로딩 스피너 → 카드 실루엣 스켈레톤(커버+제목+3스탯).
  - **C3 스탬프 공유 페이지 중복 fetch 제거** — `app/share/stamps/[id]/page.tsx`. `fetchStamp`·`fetchUserDisplay`를 React `cache()`로 래핑(generateMetadata+렌더 중복 호출 제거, 크롤러 OG 페이지 DB 호출 절반↓). 미사용 import(`ShareCtaSection`) 정리.
  - **C1 스탬프 공유 kind 정정 + 유입 추적 보완** — `lib/share/share-channels.ts`(`ShareKind`에 `"stamp"` 추가), `stamp-share-dialog.tsx`(kakao/native `kind: "note"→"stamp"`), `components/share/referral-tracker.tsx`(`/share/stamps/` 분기 추가 — 스탬프 유입 추적 누락 갭 보완).
- **검증 후 의도적 스킵 (불필요·위험 회피):**
  - **A2** `getCachedReadingStats` 제거 — `RecentBooksSection`에서도 `recentBooks` 위해 필요 + React cache() 동일 요청 디듀프 → 히어로에서 빼도 집계는 1회 실행되어 실익 없음.
  - **C2** version 파라미터 — `StampShareData`에 `updated_at` 없고 `createdAt`은 수정 시 불변 + OG URL이 이미 stamp id로 캐시 분리 → 실익 없음.
- **검증:** `tsc --noEmit` ✅ (에러 0), ESLint 클린 ✅ (에러/경고 0)
- **후속 과제:** `getPopularBooks` 500행 JS 집계 → RPC/머티리얼라이즈드 카운트(중규모 범위 밖, 책 0권 사용자에만 실행되어 영향 제한적)
- **플랜 문서:** `~/.claude/plans/idempotent-waddling-dragonfly.md`

---
