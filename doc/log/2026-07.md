# 2026-07 Prompt Log — Habitree ReadingTree

---

### 2026-07-12 — 정식 도메인 read.habitree.io 전면 적용 (공유 링크·리다이렉트·메타데이터)
- **카테고리:** #도메인 #공유 #SEO #인프라 #미들웨어
- **중요도:** P0 (도메인 이전)
- **지시:** "Vercel에 read.habitree.io 새 도메인 설정 완료. 공유 링크와 사용자 사용 시 기존 readingtree-tan.vercel.app이 아닌 새 도메인으로 전체 적용."
- **① 정식 도메인 단일 소유(`lib/utils/url.ts`):** `CANONICAL_APP_URL = "https://read.habitree.io"` 상수 신설 + 하드코딩 4곳 교체. **레거시 가드**: `NEXT_PUBLIC_APP_URL`이 구 도메인으로 남아 있으면 무시하고 정식 도메인 사용(Vercel env 갱신 누락 안전망). production에서 `VERCEL_URL`(배포별 생성 URL)보다 정식 도메인 우선. **브라우저 프로덕션 번들 안전망**(클라이언트에 VERCEL env 미노출 대비). 디버그 console.log 제거.
- **② 구 도메인 308 리다이렉트(`proxy.ts`):** `isLegacyProdHost()`로 host 판별 → 경로·쿼리 유지한 채 정식 도메인으로 영구 리다이렉트. 과거 공유 링크·북마크·검색 유입 수렴. vercel.app 프리뷰 도메인은 영향 없음(정확한 host 매칭).
- **③ 공유 링크 전면 정식 도메인화:** `window.location.origin` 기반이던 7개 공유 진입점(노트 simple-share-dialog, AI리포트 report-share-dialog, 완독 completion-celebration-card, 책장 bookshelf-share-button, 그룹초대 invite-link-dialog·settings/page, 독서시간 reading-time-link-share)을 전부 `getAppUrl()`로 통일 — 구 도메인 접속 중에도 항상 새 도메인 링크 공유.
- **④ 메타데이터/SEO:** `app/layout.tsx`(metadataBase·og url), `app/sitemap.ts`, `app/robots.ts` 하드코딩 제거 → `getAppUrl()`. `lib/og/constants.ts` OG 도메인 표기 갱신. **DB `og_settings.domain`**(habitree.app으로 남아 있던 활성 행) → read.habitree.io 로 UPDATE.
- **⑤ 결제:** Polar 체크아웃 2개 라우트의 자체 env 조합 → `getAppUrl()` 재사용(successUrl 정식 도메인 보장).
- **수동 후속 조치(대시보드):** Vercel env `NEXT_PUBLIC_APP_URL`=https://read.habitree.io 갱신(+GitHub Actions secret 동일), Supabase Auth Site URL·Redirect URLs에 read.habitree.io 추가, Kakao Developers Web 플랫폼 도메인에 read.habitree.io 등록.
- **검증:** `tsc` 통과, eslint 신규 오류 0(기존 7건은 HEAD에도 존재하는 사전 존재분 확인), `next build` 성공(Proxy 미들웨어·전 라우트 정상 등록).
- **변경 파일:** `lib/utils/url.ts`, `proxy.ts`, `app/layout.tsx`, `app/sitemap.ts`, `app/robots.ts`, `lib/og/constants.ts`, `components/share/simple-share-dialog.tsx`, `components/books/report-share-dialog.tsx`, `components/books/completion-celebration-card.tsx`, `components/books/reading-time-link-share.tsx`, `components/bookshelves/bookshelf-share-button.tsx`, `components/groups/invite-link-dialog.tsx`, `app/(main)/groups/[id]/settings/page.tsx`, `app/api/checkout/polar/route.ts`, `app/api/checkout/polar/subscription/route.ts`, `scripts/capture-payment-flow.mjs`.

---

### 2026-07-12 — 독서시간 이미지 카드 v3.1: 텍스트 잘림 수정 + 서비스 로고 적용 + 디자인 정제
- **카테고리:** #공유 #records #독서시간 #디자인 #버그
- **지시:** "이미지 복사의 텍스트가 조금씩 잘리는 부분과 왼쪽 위 로고를 우리 서비스 로고와 동일하게 적용, 자연스러운 모습으로 최신 디자인 트렌드에 맞게 고도화."
- **잘림 원인:** 책 제목/저자에 쓴 CSS `line-clamp-2`/`line-clamp-1`(-webkit-box + **overflow hidden**)을 html2canvas가 캡처할 때 웹폰트 메트릭 차이로 텍스트를 위로 밀어 그려 **글자 상단이 잘림**(큰 폰트일수록 심함 — 20px 제목만 눈에 띄게 잘리고 12px 저자는 미세). 캡처 DOM에서는 line-clamp 금지.
- **수정(`reading-time-share-button.tsx`):** ① 제목/저자를 `truncateText()` **JS 절단으로 교체**(제목 34자·저자 26자 — 374px 폭에서 2줄/1줄 이내 보장), overflow hidden 제거 + 제목 `leading-[1.4]`로 여유. ② 🌳 이모지 워드마크 → **서비스 로고와 동일한 lucide Trees 마크**(`export-trees-icon-png.mjs`와 동일 패스의 인라인 SVG, 캡처 안정성 위해 stroke 색 직접 지정 #5ec496) + 단색 볼드 `ReadTree`(사이드바/헤더와 동일 구성). ③ 정제: 오로라 그라데이션 알파 완화(0.32→0.26)로 자연스러운 톤, 상단 셴(sheen) 레이어로 유리 질감(filter 불가 → 투명 그라데이션), 리듬 바 14→18px로 존재감 강화. 카드 주석에 line-clamp 금지 규칙 명문화.
- **검증:** `tsc`·eslint 통과. **html2canvas 1.4.1 실캡처 재현 테스트**(scratchpad HTML + 로컬 http-server + Playwright): 캡처 결과에서 Trees 인라인 SVG 정상 렌더 + JS 절단 제목 잘림 없음 확인.
- **변경 파일:** `components/books/reading-time-share-button.tsx`.

---

### 2026-07-12 — 내 기록 삭제 기능: 피드 카드 삭제 버튼 + 편집 화면 내 즉시 삭제
- **카테고리:** #records #기록 #삭제 #UX
- **지시:** "내기록 페이지에서 삭제 버튼과 편집내에서도 쉽게 삭제할 수 있는 기능 추가."
- **배경:** 삭제 서버 액션(`deleteProgressLog`·`deleteNote`)은 이미 존재했으나 **통합 기록 피드(내 기록)에는 편집만 노출**되고 삭제 진입점이 없었음(삭제 UI는 책 상세 시간 탭·독서속도 상세에만 있었음). 편집 화면(RecordSheet attach / 노트 편집 폼)에도 삭제 버튼이 없어 편집 도중 지우려면 화면을 나가야 했음.
- **① 피드 카드 삭제(`unified-record-card.tsx`·`unified-record-feed.tsx`):** 카드 우측 편집 옆에 휴지통 아이콘 버튼 추가(`onDelete` 옵셔널 prop — 미지정 시 숨김). 피드가 `source`별로 분기(`reading_log`→`deleteProgressLog`, `note`→`deleteNote`) 후 **낙관적 제거 + 확인 AlertDialog**(사진 첨부 시 문구 분기). `reading-time-tab`의 검증된 삭제 패턴 재사용, 서버 `revalidatePath`로 새로고침 시에도 일관.
- **② 시간기록 편집 시트 내 삭제(`record-attach-step.tsx`):** 저장 액션 하단에 "이 기록 삭제" 텍스트 버튼 + 확인 다이얼로그. 삭제 성공 시 시트 `reset()` → 피드의 시트-닫힘 `router.refresh()`로 반영. 기존 `useTransition`(isPending) 재사용해 저장/삭제 동시 방지.
- **③ 노트 편집 폼 내 삭제(`note-edit-form.tsx`):** 기존 `NoteDeleteButton`을 `label`·`triggerVariant`·`triggerClassName` 옵셔널 prop으로 확장(기존 아이콘 전용 사용처 무영향)해 편집 폼 저장 버튼 위에 전폭 삭제 버튼으로 재사용. 삭제 후 `/notes`로 이동.
- **검증:** `tsc --noEmit` 통과(exit 0), 변경 파일 eslint 0 error(기존 warning 6건은 무관한 사전 존재분).
- **변경 파일:** `components/records/unified-record-card.tsx`, `components/records/unified-record-feed.tsx`, `components/records/record-attach-step.tsx`, `components/notes/note-edit-form.tsx`, `components/notes/note-delete-button.tsx`.

---

### 2026-07-12 — 독서기록 공유: PC에서 네이티브 공유 시트 대신 링크 즉시 복사
- **카테고리:** #공유 #records #독서시간 #PC #버그
- **지시:** "PC 버전에서 독서기록 공유 시 링크가 그대로 복사되도록. Windows/Mac 등 OS에 관계없이 적용."
- **원인:** `reading-time-link-share.tsx`가 `navigator.share` 존재 여부만으로 1차 네이티브 공유를 시도 → Windows Chrome/Edge는 `navigator.share`가 존재해 **OS 공유 다이얼로그**가 뜨는데, 환경에 따라 "다시 시도하세요. 공유할 수 있는 일부 방법만 표시됩니다."로 실패. 실패해도 AbortError가 아니어서 폴백이 돌지만 UX가 이미 깨진 상태.
- **수정(`reading-time-link-share.tsx`):** 기존 유틸 `isMobile()`(`lib/utils/device.ts`, UA 기반 — stamp/recap 공유 다이얼로그의 `isNativeShareAvailable() && isMobile()` 패턴과 동일)을 재사용해 **네이티브 공유는 모바일에서만** 시도. PC는 OS(Windows/Mac/Linux) 무관하게 곧장 클립보드 복사(clipboard.writeText → execCommand 폴백)로 진입, "공유 링크를 복사했어요" 토스트 + 복사됨 상태 표시. 모바일 동작은 기존 그대로.
- **검증:** `tsc --noEmit` 통과(exit 0).
- **변경 파일:** `components/books/reading-time-link-share.tsx`.

---

### 2026-07-12 — 카카오 로그인 후 재로그인 화면 노출 버그 수정 (OAuth 콜백 세션 쿠키 전달)
- **카테고리:** #인증 #OAuth #카카오 #쿠키 #Next16 #버그
- **중요도:** P1
- **지시:** "카카오 로그인 뒤에 로그인 페이지가 다시 나오고, 로그인은 진행되어 있지만(세션 존재) 재로그인 화면이 나오지 않게 오류 검토·수정."
- **진단:** `login_logs`가 **전부 success**(오늘 07-12 카카오 포함) → `exchangeCodeForSession`·`getUser`는 서버에서 성공, 세션은 확립됨. 그런데 로그인 화면이 다시 뜸 = **브라우저/후속 요청이 세션 쿠키를 못 받음**. 핵심 비대칭 발견: **미들웨어**(`lib/supabase/middleware.ts`)는 쿠키를 응답 객체에 직접(`response.cookies.set`) 심는 정석 패턴인데, **콜백**(`app/callback/route.ts`)은 `createServerSupabaseClient()`로 `next/headers` 쿠키 스토어에만 쓰고 그와 **별개로 손수 만든 `NextResponse.redirect()`**를 반환. **Next.js 16 라우트 핸들러에서는 cookies() 스토어 변경분이 수동 생성한 NextResponse.redirect에 안정적으로 병합되지 않아** 세션 쿠키가 브라우저에 안 실림 → 미들웨어 `getUser()` null → 로그인 화면 재노출.
- **수정(`app/callback/route.ts`):** 콜백 전용 Supabase 클라이언트를 인라인 생성. 세션 저장은 **기존과 동일하게 next/headers 쿠키 스토어를 유지**(같은 요청에서 호출되는 `grantWelcomeBonus`·`processReferralOnSignup` 등 RLS 서버 액션이 세션을 읽어야 함 — 회귀 방지)하되, `setAll`에서 변경분을 `cookiesToSet`에 추가로 수집. 새 헬퍼 `buildRedirect(target)`가 모든 리다이렉트 응답에 수집된 쿠키를 직접 실어 **브라우저 전달을 보장**. 기존 10개 리다이렉트 지점(성공/온보딩/각 에러/이메일 인증/복구)을 전부 `buildRedirect`로 통일, 중복 `getAppUrl()` 호출을 상단 `baseUrl` 단일화. 환경변수 누락 가드 추가.
- **범위 판단:** `signInWithKakao` 서버 액션의 PKCE code-verifier 쿠키 전달은 정상(교환 성공이 증거)이라 미변경. 클라이언트 `auth-context`는 쿠키만 오면 `onAuthStateChange`로 복구되므로 미변경(원인은 쿠키 전달 자체).
- **검증:** `tsc --noEmit` 통과(exit 0). (실제 카카오 OAuth 왕복은 배포 환경에서 재확인 권장.)
- **변경 파일:** `app/callback/route.ts`.

---

### 2026-07-10 — 배경음악 곡 전환 겹침 완전 제거: 교차페이드 → 순차 딥 + 파일 전수 실측
- **카테고리:** #음악 #버그 #곡전환 #오디오 #실측검증
- **지시:** "아직 중간중간에 겹치는 구간들이 있는데 음악파일 전체 디테일 점검해서 완벽하게 업데이트 적용."
- **파일 전수 실측(ffmpeg/ffprobe, 4채널 22파트 전부 다운로드):** 각 파트 실측 duration vs `genres.ts` 메타 = **전부 Δ≈0.000s 일치**. `silencedetect`로 검출한 실제 곡 경계도 큐(cue) 경계와 트림 여유(`start_silence=0.5`) 범위 내 일치(반복되는 0.51s는 의도된 도입부 무음). → **파일·메타 완전 정상, 드리프트/concat 손상/파일 내 겹침 없음.** 겹침은 파일이 아니라 재생 로직 문제로 확정.
- **원인(잔존 겹침):** v3의 **equal-power 교차페이드**가 설계상 매 곡 경계에서 구곡 꼬리와 신곡 머리를 **~0.45s 동시 재생**. 곡 사이 무음이 없는 loud-to-loud 경계(토카타와 푸가·에그몬트 서곡·산왕의 궁전에서 등 `—`로 검출된 경계)에서 두 곡이 실제로 겹쳐 들림.
- **수정(`music-mini-player.tsx`):** 교차페이드를 **순차 딥(dip)**으로 교체 — `dipTransition(fromEl, toEl)`: ①구곡을 무음까지 페이드아웃(280ms) 후 **정지**, ②그 뒤 신곡 페이드인(380ms). 신곡은 t=0에 무음으로 재생 시작해 도입부(대개 무음)를 소진, 구곡 정지 시점에 올려 공백 최소화. **두 곡 음악이 동시에 들리는 순간 0** (볼륨 엔벨로프 수치 시뮬레이션으로 overlap 0틱 검증). 대부분 곡은 앞/뒤 트림 무음이 있어 딥이 공백 없이 자연스럽게 이어짐. `CROSSFADE_LEAD_S` 0.55→0.45(구곡이 경계 즈음 무음 도달). 07-09 누출차단/레퍼리/이중전환 가드/프리로드 판정보강은 유지.
- **검증:** type-check·eslint 통과. 4채널 22파트 실측 일치. 딥 엔벨로프 시뮬레이션 겹침 0 확인. (최종 청감은 배포 앱 권장 — 파일 재업로드 불필요, 코드만.)
- **변경 파일:** `components/music/music-mini-player.tsx`.

---

### 2026-07-09 — 독서시간 공유 v4: 공개 랜딩 페이지 신설 + 이미지 카드 표지 렌더 버그 수정
- **카테고리:** #공유 #records #독서시간 #디자인 #OG #공개페이지
- **지시:** "독서시간 공유·이미지복사의 책표지가 제대로 보이고 가시성·정렬 개선. 공유 링크 클릭 시 링크가 복사되고, 그 페이지는 로그인 없이도 타인이 책 정보·독서시간을 한눈에 볼 수 있는 자랑하고 싶은 대시보드형으로."
- **핵심 진단:** ① 이미지 카드가 표지를 `next/image`에 **원본 URL 직접 바인딩** → 외부 표지(카카오/알라딘 등) CORS로 html2canvas 캡처 시 **빈칸**. ② 링크 공유가 `window.location.href`(로그인 필수 `app/(main)/books/[id]`)를 공유 → 받은 사람이 **로그인 벽**에 막힘. ③ 독서시간용 **공개 공유 페이지 부재**(완독/스탬프/노트/리포트는 이미 `app/share/*`에 완비).
- **① 이미지 카드 표지 수정(`reading-time-share-button.tsx`):** 표지를 이미 존재하던 `getProxiedImageUrl()`(same-origin `/api/image-proxy`)로 로드 + 순수 `<img crossOrigin="anonymous">`로 교체 → html2canvas 캡처 정상화. 표지 **66×90 → 88×128 확대**(책등 음영+광택 레이어로 프리미엄 질감), 책정보 행 상단정렬·여백 재조정으로 가시성·정렬 개선(540px 캔버스 오버플로우 없이).
- **② 링크 공유 공개 URL 전환(`reading-time-link-share.tsx`):** 공유 대상을 `${origin}/share/reading-time/[userBookId]`로 변경(`userBookId` prop 신설, `reading-time-tab.tsx`에서 주입). navigator.share→clipboard→execCommand 3단 폴백 유지. 토스트에 "누구나 열어볼 수 있어요" 명시.
- **③ 공개 랜딩 페이지 신설(`app/share/reading-time/[userBookId]/page.tsx`):** 완독 카드(`app/share/completions`)와 동일하게 **admin(service_role) 클라이언트로 링크만으로 공개 조회**(별도 is_public·RLS·마이그레이션 불필요, 링크=의도된 공개). `user_books`→`books` 조인 + `reading_logs` 집계(`summarizeReadingTime` 재사용) + 소유자 이름/아바타. **디자인:** 다크 오로라 히어로(이미지 카드와 톤 일치)—소유자·표지(104~116px)·오버사이즈 총시간·진행률 바 / 통계 타일 4종(세션·평균·독서한 날·읽은 페이지 or 최장 세션, 중복 제거) / 독서 기간·스탬프 배지 / 최근 세션 타임라인 6건 / 가입 CTA(200P). "자랑하고 싶은" Wrapped/Strava형 공유 랜딩.
- **④ OG 미리보기 신설(`opengraph-image.tsx`):** 카톡/SNS 카드(1200×630). 좌 표지(`OgBookCoverFrame`)+우 총 독서시간 히어로+세션/페이지/진행률 칩. `lib/og/*` 헬퍼 재사용, `react-hooks/error-boundaries` 규칙 준수 위해 데이터 조회(try/catch)와 JSX 구성을 분리. `lib/og/meta.ts`에 `"reading-time"` 종류 추가.
- **스킬/에이전트 검토:** dataviz·artifact-design 스킬은 Claude 아티팩트/차트용이라 직접 대상 아님 → 기존 `app/share/*` OG·공개페이지 인프라를 정본 패턴으로 채택, 최신 SNS 공유 랜딩(Spotify Wrapped/Strava) 트렌드 반영.
- **검증:** type-check(tsc)·eslint(변경 6파일) 통과, `next build` exit 0 — 신규 라우트 2개(`/share/reading-time/[userBookId]` + `/opengraph-image`) 서버 라우트로 정상 등록. (실데이터 청감/시각 확인은 배포 앱에서 실제 userBookId로 권장.)
- **변경 파일:** `app/share/reading-time/[userBookId]/page.tsx`(신규), `app/share/reading-time/[userBookId]/opengraph-image.tsx`(신규), `components/books/reading-time-share-button.tsx`, `components/books/reading-time-link-share.tsx`, `components/books/reading-time-tab.tsx`, `lib/og/meta.ts`.

---

### 2026-07-09 — 배경음악 곡 전환 시 여러 곡이 겹쳐 들리는 현상 수정
- **카테고리:** #음악 #버그 #곡전환 #교차페이드 #오디오
- **지시:** "업데이트된 음악파일 재생 시 여러 음악이 중복되서 들리는 현상 원인분석·개선 적용."
- **원인:** v3 곡 전환 개편(2026-07-08, `e8c66aa`)에서 하드컷(remaining≤0.05)을 경계-전 교차페이드로 바꾸며 **프리로드 미완 시 "무음 컷 대신 활성 스트림을 그대로 흘려보내는" 폴백**을 넣었는데, 채널 파트 파일은 곡들이 **원본(파일) 순서**로 이어져 있고 재생 순서는 **셔플 큐**라, 경계를 넘겨 흐르면 *파일상 다음 곡*(셔플 다음과 다른 곡)이 들리다가 프리로드 완료 시 *셔플 다음 곡*으로 교차페이드 → **서로 다른 두 곡이 뒤섞여 겹쳐 들림**. 프리로드 준비 판정 취약(대용량 CBR 파트의 깊은 seek·일시정지된 비활성 엘리먼트 버퍼 회수·`canplay` 리스너 등록 전 발생)으로 이 폴백이 자주 발동. (라이브 파트 파일 크기·genres.ts 메타 일치 검증 완료 — 파일 자체는 정상, 순수 전환 로직 문제.)
- **수정(`music-mini-player.tsx`):**
  - **누출 차단:** 경계를 넘긴(remaining≤0) 뒤 프리로드 미완이면 활성 스트림을 **음소거**(파일-다음 곡이 소리로 새지 않게). 이후 스왑은 구 스트림을 되살리지 않고 **신곡만 페이드인**(`swapToPreloaded`에 `fadeFromActive` 인자 추가, 폴백 스왑·스킵 시 `!pendingSwap`).
  - **이중 재생 레퍼리:** 전환 중이 아닐 때 비활성 엘리먼트가 재생 중이면 즉시 정지 — 중단된 교차페이드 등으로 소리가 새어도 다음 timeupdate(≤250ms)에 회수.
  - **이중 전환 가드:** 교차페이드 중 구 엘리먼트의 `ended`는 무시(큐 이중 진행·전환 중단 방지).
  - **프리로드 준비 판정 보강:** `canplay`/`canplaythrough`/`seeked` 다중 신호 + 즉시 1회 확인 → 이벤트를 놓쳐 `ready`가 영영 false로 남는 것 방지(무음 갭 대신 깔끔한 교차페이드 유지).
- **검증:** type-check(tsc)·eslint 통과. 라이브 Supabase 파트(piano-1=43,280,550B ≈ 1352.49s×256k) 메타 일치 확인. (최종 청감 확인은 dev/배포 앱에서 곡 전환 수 회 통과 필요.)
- **변경 파일:** `components/music/music-mini-player.tsx`.

---

### 2026-07-08 — 독서시간 공유 카드 v3 리디자인 + 링크 네이티브 공유(Web Share)
- **카테고리:** #공유 #records #독서시간 #디자인 #SNS
- **지시:** "독서시간의 링크와 이미지를 SNS 공유용 목적으로 디자인을 최신 트렌드에 맞게 업데이트 기획해서 반영 적용."
- **트렌드 방향:** 2026 SNS 공유 카드의 지배적 흐름인 **"Wrapped형 데이터 스토리"**(Spotify Wrapped·iOS 스크린타임·Duolingo 결산) 채택 — 숫자 1개가 아니라 *리듬을 보여주는 미니 차트*를 후킹 요소로.
- **이미지 카드 v3(`reading-time-share-button.tsx`):** ① 정적 "최근 기록 2건" 텍스트 리스트 제거 → **최근 7일 독서 리듬 바 차트** 신설(로컬 날짜 기준 요일별 독서량 집계, 오늘 밝은 그라데이션 강조, 우측 주간 합계). ② 브랜드 워드마크를 **상단 헤더로 승격**, 하단은 태그라인 + `#독서기록 #ReadTree` SNS 해시태그. ③ 히어로 숫자 확대(42→48px)·오로라 그라데이션 정제(탁한 앰버 축소, 에메랄드→틸 스포트라이트). html2canvas 제약(box-shadow·filter·blur 미지원) 그대로 준수 — 그라데이션/보더/rgba 레이어만.
- **링크 공유(`reading-time-link-share.tsx`):** URL 단순 복사 → **Web Share API(navigator.share) 우선**(모바일 네이티브 공유 시트로 인스타/카카오 바로 전송, 제목·문구·URL 포함) + 데스크톱·미지원은 클립보드 복사(execCommand 폴백). 아이콘/라벨 `링크 복사`→`공유`(Share2). 사용자 취소(AbortError)는 폴백 없이 조용히 종료.
- **검증:** type-check·eslint 통과. Playwright 실검증 — 임시 라우트에서 최악 높이 케이스(2줄 긴 제목 + 스탬프 라인 + 7일 전부 활동)로 카드 렌더 스크린샷. 1차 렌더 시 푸터가 540px 밖으로 잘림 발견 → 커버/히어로/바 높이·세로 마진 축소로 콘텐츠 538px(2px 여유)까지 조정, 푸터(태그라인+해시태그) 정상 노출 확인.
- **변경 파일:** `components/books/reading-time-share-button.tsx`, `components/books/reading-time-link-share.tsx`.

---

### 2026-07-08 — 게스트 독서 달력: 관리자 실제 데이터를 당월로 리매핑해 채움
- **카테고리:** #게스트 #샘플데이터 #달력 #대시보드
- **지시:** "비로그인 사용자는 처음에 독서달력에 데이터가 채워져 보이게, 실제 관리자 데이터를 참조하여 당월 데이터로 보이도록. 달력 부분만 채우고 최대한 채워서."
- **문제:** 게스트 홈 달력은 `getSampleMonthlyActivities(당월)`로 관리자가 **그 달에 작성한 노트만** 조회 → 관리자가 이번 달 기록이 적으면 빈 결과 → 기본 표지 5권짜리 일반 데모(`generateDemoMonthlyActivities`)로 폴백. 실제 책·표지가 안 보였음.
- **해결:** `getSampleFilledMonthlyActivities(year, month)` 신설(`app/actions/sample.ts`) — 관리자 실제 노트 **전체(최근 500개, 기간 제한 없음)**를 하루 단위 번들(실제 책·표지·기록 타입)로 그룹화(created_at DESC → 최근 날짜순), 대상 월의 날짜(현재 월이면 1~오늘, 과거 월이면 1~말일)에 최근순으로 매핑. `min(실제 번들 수, 채울 날짜 수)`만큼만 채워 **가짜 복제 없이 실제 데이터로 최대한** 채움. 데이터 0건일 때만 데모 폴백.
- **연결:** `tertiary-zone-wrapper.tsx`(게스트 SSR 진입)·`tertiary-zone-client.tsx`(게스트 월 이동) 두 호출부를 새 함수로 교체. 사용처 없어진 기존 `getSampleMonthlyActivities` 제거(데드코드 정리).
- **범위:** 게스트는 `isGuest`(≠`isFirstUser`)라 "샘플 안내 카드" 없이 달력만 채워진 채 노출 — 지시("달력 부분만") 부합. `/sample` 라우트는 홈 리다이렉트라 진입점은 홈 하나.
- **실검증(Supabase 프로덕션 read-only):** 관리자 1명(Nevertheless, 노트 252개 전부 표지 있음, 서로 다른 활동일 187일). 함수 로직 SQL 시뮬레이션 → 오늘(7/8) 기준 최근 활동일 8개(7/7·6/18·6/17…)가 7/8~7/1로 매핑, 전부 실제 표지 보유 확인. type-check 통과, vitest 59개 통과(auth/points 2스위트는 기존 vi.mock 호이스팅 이슈로 무관 실패).
- **변경 파일:** `app/actions/sample.ts`(getSampleFilledMonthlyActivities 신설·getSampleMonthlyActivities 제거), `components/dashboard/tertiary-zone-wrapper.tsx`, `components/dashboard/tertiary-zone-client.tsx`.

---

### 2026-07-07 — 독서시간 공유: 즉시 복사 전환 + 공유 카드 디자인 전면 개편
- **카테고리:** #공유 #records #독서시간 #디자인 #클립보드
- **지시:** "독서시간의 링크공유와 이미지공유가 복사형태로 바로 복사되어 적용되도록, 이미지를 최신 트렌드에 맞게 디자인 전면개편."
- **링크 복사(`reading-time-link-share.tsx`):** Web Share 시트 우선 → **클릭 즉시 클립보드 복사**로 전환(execCommand 폴백). 라벨 "링크 공유"→"링크 복사".
- **이미지 복사(`reading-time-share-button.tsx`):** Safari/iOS 대응 핵심 수정 — html2canvas 캡처를 await 한 뒤 clipboard.write 하면 사용자 제스처가 만료되어 다운로드로 폴백되던 것을, **ClipboardItem 에 캡처 Promise 를 동기로 넘기는 방식**(`copyImagePromiseToClipboard` 신설, `lib/utils/clipboard.ts`)으로 "바로 복사" 성공률 개선. 캡처 실패(throw)와 클립보드 미지원(false→다운로드 폴백)을 구분.
- **카드 디자인 v2:** 라이트 파스텔+blur 장식 → **다크 오로라 그라데이션 + 글래스 벤토 타일 + 오버사이즈 히어로 숫자**(시간/분 크기 분리, 에메랄드 액센트). 읽기 진행률 바(totalPages 기준 %), 커버 radial 글로우, 세션/평균/페이지(없으면 기록일) 3타일, Recent 2행(사진 기록은 amber 도트), 브랜드 푸터. html2canvas 제약(box-shadow·filter·backdrop-blur 미지원) 회피 — 그라데이션/보더/rgba 레이어만 사용.
- **검증:** Playwright 실검증 — 임시 페이지에서 카드 렌더 스크린샷 확인(오버플로우 1회 수정: Recent 3→2행, 커버/히어로 축소), **이미지 복사 클릭→클립보드에 image/png(1080×1080) 적재 확인**, 링크 복사 클릭→URL 적재 확인. type-check·eslint·`next build` 통과.
- **부수 수정:** 음악 플레이어 컨트롤러 분리 리팩토링에서 누락된 `MusicGenre` 타입 임포트 복구(`music-mini-player.tsx`).

---

### 2026-07-07 — 독서 시트 로딩 성능 + 예상시간 수동입력 + 기록 상세 추가 고도화 + 공유 카드 폴백·잘림 개선
- **카테고리:** #성능 #번들 #기록 #공유 #캡처
- **중요도:** P1
- **지시:** "독서를 누르면 페이지 로드가 오래 걸리는 부분 개선, 예상시간 수동 입력 + 이전 시간 자동저장 반영, 시간만 등록한 기록에도 상세 정보 추가 입력 고도화, 스탬프 공유 시 이미지 없을 경우 자연스럽게, 카드 이미지 복사 시 잘림 개선."
- **성능(독서 시트):** ① 헤더가 `MusicToggleButton`을 music-mini-player(오디오 엔진 500+줄 → `lib/music` 경유 19KB 채널 데이터)에서 정적 import해 **전 페이지 초기 번들에 음악 모듈 전체가 포함**되던 문제 → 토글 버튼(`music-toggle-button.tsx`)과 컨트롤러 레지스트리(`music-controller.ts`)를 경량 모듈로 분리해 코드 스플리팅 복원. ② RecordSheet lazy 청크를 LazyOverlays에서 **requestIdleCallback 프리로드** — "독서" 첫 탭의 fetch+파싱 지연(100~300ms) 제거. ③ `getActiveSession` 폴링이 레이아웃 상주 훅 인스턴스(사이드바·모바일내비·인디케이터)마다 독립 실행되던 것을 **모듈 레벨 in-flight 공유 + 10s TTL 디둡**으로 통합(브로드캐스트/refresh는 force로 즉시 갱신).
- **예상시간:** 프리셋(15/25/45/무제한) 옆 **분 단위 직접 입력**(1~600분) 추가. 시작 성공 시 `localStorage("readtree:last-target-seconds")`에 저장, 다음 시작 시 자동 적용(우선순위: prefill > 마지막 값 > 25분).
- **기록 상세 추가(attach):** `getReadingLogForEdit` 액션 신설 — RecordAttachStep이 마운트 시 기존 로그의 **메모·사진·페이지를 프리필**. 시간만 남긴 기록에 상세를 더할 때 기존 메모가 빈 폼으로 **덮어써지던 문제 해소**, 기존 사진 유지된 채 추가 가능. 편집 대상 기록의 독서 시간·날짜 요약 표시. 시트 문구 "사진 추가" → "기록 상세 추가".
- **스탬프 공유 폴백:** 사진 없음+표지 있음 → 아이콘 대신 **실제 표지를 중앙에 책처럼 연출**(블러 배경+오버레이+ring/shadow). 둘 다 없음 → 그라데이션+**책 제목 타이포그래피** 중심.
- **카드 캡처 잘림:** 스탬프/결산 다이얼로그가 html2canvas를 옵션 없이 호출(화면 렌더 크기 저해상 캡처, `background` 오타 키로 배경 무시, 폰트 대기 없음) → 노트 공유(simple-share-dialog)에서 검증된 패턴을 **공통 유틸 `lib/utils/capture-card.ts`(captureElementToPngBlob)**로 추출: `document.fonts.ready` + 이미지 load/decode 대기 + rAF 안정화 + 실측 크기 기반 width/height/scale(1080px 폭) + `backgroundColor` + onclone crossOrigin. 두 다이얼로그 적용.
- **검증:** type-check 통과, vitest 20개(use-record-sheet 6 + sessions 14) 통과.
- **변경 파일:** `components/music/music-controller.ts`(신규), `components/music/music-toggle-button.tsx`(신규), `components/music/music-mini-player.tsx`, `components/music/music-only-sheet.tsx`, `components/layout/header.tsx`, `components/layout/lazy-overlays.tsx`, `hooks/use-reading-session.ts`, `components/records/record-start-step.tsx`, `components/records/record-attach-step.tsx`, `components/records/record-sheet.tsx`, `app/actions/progress.ts`(getReadingLogForEdit), `components/stamps/stamp-share-card.tsx`, `components/stamps/stamp-share-dialog.tsx`, `components/recap/recap-share-dialog.tsx`, `lib/utils/capture-card.ts`(신규).

---

### 2026-07-07 — 배경음악 v2: 음질·다양성·셔플 재생 전면 개편 (4채널 103곡)
- **카테고리:** #음악 #음질 #다양성 #셔플 #Supabase #스토리지
- **중요도:** P1
- **지시:** "현재 설정되어있는 음악들을 음질개선과 다양성 개선 그리고 사용자들이 들을때마다 다른음악이 들리도록 업데이트 기획해서 적용. Supabase 연동하여 실제 데이터 작업까지 직접진행."
- **음질:** 인코딩 128k→**160kbps CBR**(원본 대부분 160~320k, 무료 티어 스토리지 1GB·에그레스 5GB 고려해 192k 대신 160k 채택). **EBU R128 측정 기반 선형 게인 정규화**(목표 -16 LUFS, 트루피크 -1.5dBTP 캡, volume 필터만 적용 → 다이내믹 무압축) — 조용한 클래식(-26 LUFS대)과 시끄러운 재즈(-10 LUFS대)의 곡 간 볼륨 편차 해소. 앞뒤 무음 트리밍(-50dB, 앞 0.5s/뒤 0.8s 여유).
- **다양성:** 2채널(클래식 49/재즈 35) → **4채널 103곡**. 🎹 피아노 30곡(138분) / 🎻 클래식 현악·관현악 24곡(152분) / 🎺 활기찬 클래식 14곡(79분) / 🎷 재즈 35곡(67분). 미사용 로컬 음원 중 128kbps 이상 19곡 추가(알비노니 아다지오, 예수 인간 소망의 기쁨, 토카타와 푸가, 쇼팽 녹턴 Op.48-1, 포레 파반, 보칼리제 등). `tracks.ts`에 `channel` 필드 신설(era 대신 채널이 분류 단일 기준).
- **셔플(들을 때마다 다른 순서):** 기존 "고정 순서+랜덤 위치 시작"(다음 곡은 항상 동일) → **곡 단위 Fisher-Yates 셔플 큐**. 채널 선택 시 큐 생성, 곡 경계에서 다음 셔플 곡의 (파트, 오프셋)을 비활성 audio에 프리로드 후 스왑(250ms 페이드인, 무중단). 큐 소진 임박 시 직전 곡 연속 중복을 피해 자동 연장. 부수 효과로 **다음 곡 스킵 버튼** + Media Session `nexttrack` 추가 가능해짐. 폴백 전환 시 이중 스킵 방지 `isTransitioning` 가드.
- **Supabase 데이터 작업:** `jazz-music` 버킷에 `v2/{channel}-{n}.mp3` 14개 파트(총 ~496MB) 업로드 완료(전부 HTTP 206 Range 검증). 기존 파일명 재사용 금지 — cacheControl 1년 immutable이라 CDN 스테일 위험. **v1 사고 발견·복구:** v1 병합 파트 `jazz-1.mp3`가 동명의 개별 곡 원본을 덮어썼던 것을 발견(2026-06-08 사고) → v1 스트림 cue(1073.685s~134s)에서 원곡 추출, `jazz-1-track.mp3`로 재업로드, tracks.ts 참조 변경.
- **후속(배포 후 필수):** v2가 프로덕션 반영된 뒤 `npx tsx scripts/cleanup-old-music-parts.ts` 실행해 v1 파트(classic-1..7, jazz-1..2, ~318MB) 삭제. 배포 전 실행 금지(운영 재생 끊김). 현재 버킷 ~977MB로 1GB 한도 내, 정리 후 ~660MB.
- **검증:** type-check·eslint·`next build` 통과. 14개 파트 + 복구 원곡 Range(206) 응답 확인.
- **변경 파일:** `lib/music/tracks.ts`(채널+19곡), `lib/music/genres.ts`(재생성), `lib/music/index.ts`(findCueIndexAt), `types/music.ts`(4채널 id), `hooks/use-music-player.ts`(셔플 큐), `components/music/music-mini-player.tsx`(곡 단위 전환+스킵), `components/music/music-only-sheet.tsx`(4채널 UI), `scripts/build-combined-music.ts`(160k+정규화+트림+v2), `scripts/cleanup-old-music-parts.ts`(신규).

---
