-- ============================================
-- 마이그레이션: groups 링크 접근 허용
-- 날짜: 2026-01-29 19:30
-- ============================================
--
-- 변경 사항:
-- 1. groups 테이블의 SELECT 정책 수정
--    - 비공개 모임도 링크로 접근 시 기본 정보 조회 가능
--    - 상세 정보(멤버, 기록 등)는 Server Action에서 제어
--
-- 목적:
-- - 비공개 모임을 초대 링크로 공유할 수 있도록 함
-- - 링크로 접근한 사용자가 참여 신청 가능
--
-- 영향받는 테이블:
-- - groups
-- ============================================

-- groups 테이블 SELECT 정책 수정
-- 모든 그룹의 기본 정보 조회 허용 (링크 접근 지원)
DROP POLICY IF EXISTS "Anyone can view public groups" ON groups;

CREATE POLICY "Anyone can view groups"
    ON groups FOR SELECT
    USING (
        -- 로그인한 사용자는 모든 그룹의 기본 정보를 볼 수 있음
        -- (링크로 접근하여 참여 신청할 수 있도록)
        auth.uid() IS NOT NULL
    );

-- ============================================
-- 주의사항:
-- - 그룹 기본 정보(name, description, is_public)만 노출됨
-- - 멤버 목록, 공유 기록 등 상세 정보는 별도 테이블에서 관리
-- - 상세 정보 접근은 Server Action에서 멤버십 확인 후 처리
-- ============================================
